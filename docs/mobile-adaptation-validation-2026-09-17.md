# 手机端适配完成记录

日期：2026-09-17

工作分支：`codex/mobile-responsive`，基于 `8c7f3bc`。本次只修改前端，未部署。

## 已完成

- 公共外壳：手机顶部导航、项目切换、全部功能抽屉；切换路由后收起菜单，桌面侧栏折叠偏好独立保留。手机停用背景 WebGL 和液态玻璃滤镜。
- 响应式断点：宽度不超过 767px；以及宽度不超过 1023px、屏幕高度不超过 500px 的横屏手机。使用同一份 `matchMedia` 查询驱动 Vue 状态，组件销毁时清理监听。
- 通用适配：安全区和动态视口高度、表单单列、筛选和工具栏换行、分页触控尺寸、弹窗视口边界及内部滚动、详情抽屉、双月日期选择器、宽表局部滚动提示。
- 工单、审批、账单、合同、租客、线索六类列表：手机使用卡片，查询筛选可折叠，全部原有字段和状态控制的操作保持可访问；桌面保留表格。卡片具有记录名称、独立的无障碍标签和 44px 操作触控区域；工单保留来源定位高亮。
- 租控管理：手机房源卡片和可折叠楼栋选择。
- 首页、详情页、剖面图、报表、收银台、应收登记、日程、工单统计、导入弹窗等复杂页面布局；多个详情描述区在手机切换为单列。
- 修复收银台固有的表格最小内容宽度撑开页面问题：桌面宽表在组件内部滚动，较窄屏幕的结算区上下排列。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| `pnpm test` | 21 个测试文件、78 项测试全部通过；改动前为 17 个文件、66 项 |
| `pnpm build` | 生产构建通过 |
| `git diff --check` | 通过 |
| 独立代码审查 | 业务列表专项复审、公共布局与复杂页面综合审查均通过 |
| 360px 页面扫描 | 105 个非详情路由未发现整页/主内容横向溢出；另检查房源、租客、合同详情 |
| 核心页面尺寸 | 390×844、430×932、844×390、768×1024、1440×1000；首页、工单、租控、收银台；发现的收银台宽度问题已修复并确认 |
| 交互检查 | 菜单开关与路由跳转、项目切换、筛选展开、工单表单必填校验及取消、收银账单选择、日期范围弹层、导入弹窗 |
| 生产构建浏览器检查 | 实际加载 `dist/assets`，确认 390px 卡片/筛选/菜单/项目切换/日期弹层及 1440px 收银台布局 |

新增自动化覆盖响应式状态与监听清理、菜单及断点切换、桌面折叠偏好、卡片内容与操作传递、空态与加载态、跨列表标签唯一性、工单手机定位高亮。

设计扫描报告两项现存桌面效果提示：侧栏宽度过渡和菜单摇摆动画。本次保留原有桌面风格；它们未用于手机导航。

## 验证边界

- 浏览器验证使用独立本地模拟接口，所有 `/api` 请求被拦截，没有向真实后端执行写操作。高频页面提供代表性数据，其他路由主要验证空态及布局宽度；不等同于逐条完成所有业务流程验收。
- 已验证桌面浏览器的多尺寸视口；尚未在真实 iOS/Android 设备、实际软键盘和线上业务数据下验收。
- 原有测试中的 jsdom WebGL/图标警告、构建中的 Sass 旧接口和大体积 chunk 警告仍存在，均不影响本次测试或构建通过。
- 没有修改后端、接口、依赖、认证或部署配置。用户原有未跟踪的物联网设计文档保持不变。
