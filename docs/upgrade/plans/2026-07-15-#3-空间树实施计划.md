# #3 统一空间树主数据 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不改动既有 building/contract/finance 逻辑的前提下，叠加一棵统一空间树主数据 `sys_space`，并让核心业务表可选挂树，支持按任意空间层级做子树聚合。

**Architecture:** 新建 `sys_space` 表（邻接表 `parent_id` + 物化路径 `path` 混合），通过 `SpaceSyncService` 把现有 project/building/floor/room 四层投影进树（一次性回填 + building CRUD 增量 + 夜间对账兜底）。5 张业务表加可空 `space_id`，双写回填，旧 `roomId` 外键原地不动。查询走只读 `/space` 接口。

**Tech Stack:** Spring Boot 3.2 + MyBatis-Plus + MySQL 8 + Flyway；无本机 JDK，编译走 maven Docker 容器。

## Global Constraints

- 后端包结构：`com.zhyq.park.<模块>.{entity,mapper,controller[,service]}`，新建模块 `space`。
- **新 mapper 必须放 `*.mapper` 结尾包**（主类 `@MapperScan("com.zhyq.park.**.mapper")` 运行期扫描，放错编译过但启动崩——批次② 502 教训）。
- 所有实体继承 `com.zhyq.park.common.base.BaseEntity`（tenant_id/create_by/create_time/update_*/version/deleted 由 `MyMetaObjectHandler` 自动填充）。
- 逻辑删除：`deleted` 字段，唯一键须带 `deleted`（项目 V9 惯例）。
- Flyway 当前最新 **V18**，本计划用 **V19**（单文件 `V19__space_tree.sql`）。
- 编译命令（本机无 JDK/mvn，Docker daemon 需先 `open -a Docker`）：
  `docker run --rm -v "$PWD":/app -v ~/.m2:/root/.m2 -w /app maven:3.9-eclipse-temurin-17 mvn -DskipTests compile`（在 `backend/` 下跑）。
- 全栈冒烟：`docker compose -f docker-compose.full.yml up -d --build`（项目根），`http://localhost`，账号 `admin / zhyq@2026`。后端 context-path `/api`。
- **零触碰原则**：不改 biz_project/building/floor/room 表结构；业务表只加可空列；对 building controller 只在 CRUD 末尾**追加**同步调用（try-catch 吞异常，不阻断主业务）。
- 无单元测试框架；纯函数（编码 derive / path 拼接）用可在 maven 容器跑的 JUnit 5（`mvn test -Dtest=...`），其余靠容器编译 + localhost 冒烟。

---

### Task 1: Flyway V19 建表与业务表加列

**Files:**
- Create: `backend/src/main/resources/db/migration/V19__space_tree.sql`

**Interfaces:**
- Produces: 表 `sys_space`（列见下）；`biz_contract` / `biz_contract_room` / `pm_work_order` / `eng_reading` / `iot_device` 各新增 `space_id BIGINT NULL` + `idx_space_id`。回填在 Task 4/7 做，本 migration 只建结构（DDL 与回填解耦）。

- [ ] **Step 1: 写 V19 迁移 SQL**

```sql
-- V19__space_tree.sql  统一空间树主数据（叠加层，不动既有四表）
CREATE TABLE sys_space (
    id          BIGINT       NOT NULL AUTO_INCREMENT,
    parent_id   BIGINT       NULL COMMENT '父节点id，根为null',
    path        VARCHAR(255) NOT NULL DEFAULT '' COMMENT '物化路径 /1/5/23/',
    level       TINYINT      NOT NULL DEFAULT 1 COMMENT '层级深度，1=根',
    type        VARCHAR(16)  NOT NULL COMMENT 'PROJECT/BUILDING/FLOOR/ROOM，预留PARK/ZONE/AREA/ASSET_LOC',
    code        VARCHAR(64)  NOT NULL COMMENT '统一空间编码',
    name        VARCHAR(128) NOT NULL DEFAULT '',
    ref_type    VARCHAR(16)  NOT NULL COMMENT 'project/building/floor/room',
    ref_id      BIGINT       NOT NULL COMMENT '旧表主键',
    sort        INT          NOT NULL DEFAULT 0,
    status      TINYINT      NOT NULL DEFAULT 1 COMMENT '1正常0停用',
    tenant_id   BIGINT       NULL,
    create_by   VARCHAR(64)  NULL,
    create_time DATETIME     NULL,
    update_by   VARCHAR(64)  NULL,
    update_time DATETIME     NULL,
    version     INT          NOT NULL DEFAULT 0,
    deleted     TINYINT      NOT NULL DEFAULT 0,
    PRIMARY KEY (id),
    UNIQUE KEY uk_ref (ref_type, ref_id, deleted),
    UNIQUE KEY uk_code (code, deleted),
    KEY idx_parent (parent_id),
    KEY idx_path (path),
    KEY idx_type (type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='统一空间树主数据';

ALTER TABLE biz_contract       ADD COLUMN space_id BIGINT NULL COMMENT '空间树节点id', ADD KEY idx_space_id (space_id);
ALTER TABLE biz_contract_room  ADD COLUMN space_id BIGINT NULL COMMENT '空间树节点id', ADD KEY idx_space_id (space_id);
ALTER TABLE pm_work_order      ADD COLUMN space_id BIGINT NULL COMMENT '空间树节点id', ADD KEY idx_space_id (space_id);
ALTER TABLE eng_reading        ADD COLUMN space_id BIGINT NULL COMMENT '空间树节点id', ADD KEY idx_space_id (space_id);
ALTER TABLE iot_device         ADD COLUMN space_id BIGINT NULL COMMENT '空间树节点id', ADD KEY idx_space_id (space_id);
```

- [ ] **Step 2: 确认列名与既有表匹配**

Run: `grep -rl "CREATE TABLE biz_contract_room\|CREATE TABLE pm_work_order\|CREATE TABLE eng_reading\|CREATE TABLE iot_device" backend/src/main/resources/db/migration/`
Expected: 命中既有建表迁移，确认表名拼写与 V19 一致，且这些表当前无 space_id 列。

- [ ] **Step 3: 容器编译（确保未破坏构建）**

Run: `docker run --rm -v "$PWD":/app -v ~/.m2:/root/.m2 -w /app maven:3.9-eclipse-temurin-17 mvn -DskipTests compile`
Expected: BUILD SUCCESS

- [ ] **Step 4: Commit**

```bash
git add backend/src/main/resources/db/migration/V19__space_tree.sql
git commit -m "feat(space): V19 建 sys_space 表 + 5 业务表加 space_id 列"
```

---

### Task 2: SpaceNode 实体 + Mapper

**Files:**
- Create: `backend/src/main/java/com/zhyq/park/space/entity/SpaceNode.java`
- Create: `backend/src/main/java/com/zhyq/park/space/mapper/SpaceNodeMapper.java`

**Interfaces:**
- Produces:
  - `SpaceNode`（`@TableName("sys_space")` extends BaseEntity）字段：`Long parentId; String path; Integer level; String type; String code; String name; String refType; Long refId; Integer sort; Integer status;`
  - `SpaceNodeMapper extends BaseMapper<SpaceNode>`，附加 `List<SpaceNode> selectSubtree(@Param("pathPrefix") String pathPrefix)`。

- [ ] **Step 1: 写 SpaceNode 实体**

```java
package com.zhyq.park.space.entity;

import com.baomidou.mybatisplus.annotation.TableName;
import com.zhyq.park.common.base.BaseEntity;
import lombok.Data;
import lombok.EqualsAndHashCode;

@Data
@EqualsAndHashCode(callSuper = true)
@TableName("sys_space")
public class SpaceNode extends BaseEntity {
    private Long parentId;
    private String path;
    private Integer level;
    private String type;      // PROJECT/BUILDING/FLOOR/ROOM
    private String code;
    private String name;
    private String refType;   // project/building/floor/room
    private Long refId;
    private Integer sort;
    private Integer status;
}
```

- [ ] **Step 2: 写 SpaceNodeMapper（放 *.mapper 包）**

```java
package com.zhyq.park.space.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.zhyq.park.space.entity.SpaceNode;
import org.apache.ibatis.annotations.Param;
import org.apache.ibatis.annotations.Select;

import java.util.List;

public interface SpaceNodeMapper extends BaseMapper<SpaceNode> {

    @Select("SELECT * FROM sys_space WHERE path LIKE #{pathPrefix} AND deleted = 0 ORDER BY level, sort")
    List<SpaceNode> selectSubtree(@Param("pathPrefix") String pathPrefix);
}
```

- [ ] **Step 3: 容器编译**

Run: `docker run --rm -v "$PWD":/app -v ~/.m2:/root/.m2 -w /app maven:3.9-eclipse-temurin-17 mvn -DskipTests compile`
Expected: BUILD SUCCESS（确认包路径 `space.mapper` 被 @MapperScan 命中）

- [ ] **Step 4: Commit**

```bash
git add backend/src/main/java/com/zhyq/park/space/
git commit -m "feat(space): SpaceNode 实体 + Mapper（含子树查询）"
```

---

### Task 3: 空间编码 derive + path 拼接纯函数（含 JUnit）

**Files:**
- Create: `backend/src/main/java/com/zhyq/park/space/service/SpaceCodec.java`
- Test: `backend/src/test/java/com/zhyq/park/space/SpaceCodecTest.java`

**Interfaces:**
- Produces: `SpaceCodec`（静态工具）：
  - `static String childCode(String parentCode, String typePrefix, String rawCode, Long fallbackId)`
  - `static String buildPath(String parentPath, Long selfId)`
  - 常量 `PREFIX_PROJECT="P"` / `PREFIX_BUILDING="B"` / `PREFIX_FLOOR="F"` / `PREFIX_ROOM="R"`。

- [ ] **Step 1: 确认 pom 有 test 依赖，否则加**

Run: `grep -n "spring-boot-starter-test" backend/pom.xml`
Expected: 若无输出，在 `<dependencies>` 加：
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-test</artifactId>
    <scope>test</scope>
</dependency>
```

- [ ] **Step 2: 写失败测试**

```java
package com.zhyq.park.space;

import com.zhyq.park.space.service.SpaceCodec;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertEquals;

class SpaceCodecTest {

    @Test
    void projectCode_usesPrefix_whenNoParent() {
        assertEquals("PA01", SpaceCodec.childCode(null, SpaceCodec.PREFIX_PROJECT, "A01", 12L));
    }

    @Test
    void childCode_appendsUnderParent() {
        assertEquals("PA01-B1", SpaceCodec.childCode("PA01", SpaceCodec.PREFIX_BUILDING, "1", 5L));
    }

    @Test
    void childCode_fallsBackToId_whenRawBlank() {
        assertEquals("PA01-B#5", SpaceCodec.childCode("PA01", SpaceCodec.PREFIX_BUILDING, "  ", 5L));
    }

    @Test
    void buildPath_root() {
        assertEquals("/7/", SpaceCodec.buildPath(null, 7L));
    }

    @Test
    void buildPath_child() {
        assertEquals("/7/23/", SpaceCodec.buildPath("/7/", 23L));
    }
}
```

- [ ] **Step 3: 运行测试确认失败**

Run: `docker run --rm -v "$PWD":/app -v ~/.m2:/root/.m2 -w /app maven:3.9-eclipse-temurin-17 mvn test -Dtest=SpaceCodecTest`
Expected: FAIL（`SpaceCodec` 不存在，编译错误）

- [ ] **Step 4: 写实现**

```java
package com.zhyq.park.space.service;

public final class SpaceCodec {
    public static final String PREFIX_PROJECT = "P";
    public static final String PREFIX_BUILDING = "B";
    public static final String PREFIX_FLOOR = "F";
    public static final String PREFIX_ROOM = "R";

    private SpaceCodec() {}

    public static String childCode(String parentCode, String typePrefix, String rawCode, Long fallbackId) {
        String seg = (rawCode == null || rawCode.isBlank()) ? "#" + fallbackId : rawCode.trim();
        if (parentCode == null || parentCode.isBlank()) {
            return typePrefix + seg;
        }
        return parentCode + "-" + typePrefix + seg;
    }

    public static String buildPath(String parentPath, Long selfId) {
        if (parentPath == null || parentPath.isBlank()) {
            return "/" + selfId + "/";
        }
        return parentPath + selfId + "/";
    }
}
```

- [ ] **Step 5: 运行测试确认通过**

Run: `docker run --rm -v "$PWD":/app -v ~/.m2:/root/.m2 -w /app maven:3.9-eclipse-temurin-17 mvn test -Dtest=SpaceCodecTest`
Expected: PASS（5 tests）

- [ ] **Step 6: Commit**

```bash
git add backend/pom.xml backend/src/main/java/com/zhyq/park/space/service/SpaceCodec.java backend/src/test/java/com/zhyq/park/space/SpaceCodecTest.java
git commit -m "feat(space): 空间编码 derive + path 拼接纯函数（含单测）"
```

---

### Task 4: SpaceSyncService — 回填/增量/删除

**Files:**
- Create: `backend/src/main/java/com/zhyq/park/space/service/SpaceSyncService.java`

**Interfaces:**
- Consumes: `SpaceNodeMapper`, `SpaceCodec`, building 四 Mapper（`com.zhyq.park.building.mapper.{ProjectMapper,BuildingMapper,FloorMapper,RoomMapper}`）。
- Produces: `SpaceSyncService`：
  - `void reconcile()` — 全量幂等回填（先父后子）。
  - `void sync(String refType, Long refId)` — 单节点 upsert。
  - `void remove(String refType, Long refId)` — 逻辑删除对应节点。
  - `Long resolveSpaceId(String refType, Long refId)` — 查 ref 对应 space id，查不到返回 null。

- [ ] **Step 1: 先确认 building 实体字段名**

Run: `grep -E "private " backend/src/main/java/com/zhyq/park/building/entity/Floor.java`
Expected: 看清 Floor 是否有 `name` 字段。若有，Step 2 里 `floorName(f)` 可直接改 `f.getName()`；若无（只有 code），保留反射兜底。Project/Building 有 code+name、Room 有 code+roomNo 已由 Room.java 确认。

- [ ] **Step 2: 写 SpaceSyncService**

```java
package com.zhyq.park.space.service;

import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import com.zhyq.park.building.entity.*;
import com.zhyq.park.building.mapper.*;
import com.zhyq.park.space.entity.SpaceNode;
import com.zhyq.park.space.mapper.SpaceNodeMapper;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

@Slf4j
@Service
@RequiredArgsConstructor
public class SpaceSyncService {

    private final SpaceNodeMapper spaceMapper;
    private final ProjectMapper projectMapper;
    private final BuildingMapper buildingMapper;
    private final FloorMapper floorMapper;
    private final RoomMapper roomMapper;

    /** 全量回填：先父后子，保证 parent/path 正确。幂等。 */
    @Transactional
    public void reconcile() {
        for (Project p : projectMapper.selectList(null)) syncProject(p);
        for (Building b : buildingMapper.selectList(null)) syncBuilding(b);
        for (Floor f : floorMapper.selectList(null)) syncFloor(f);
        for (Room r : roomMapper.selectList(null)) syncRoom(r);
        log.info("[space] reconcile done");
    }

    public void sync(String refType, Long refId) {
        switch (refType) {
            case "project"  -> syncProject(projectMapper.selectById(refId));
            case "building" -> syncBuilding(buildingMapper.selectById(refId));
            case "floor"    -> syncFloor(floorMapper.selectById(refId));
            case "room"     -> syncRoom(roomMapper.selectById(refId));
            default -> log.warn("[space] unknown refType {}", refType);
        }
    }

    public void remove(String refType, Long refId) {
        SpaceNode n = findByRef(refType, refId);
        if (n != null) spaceMapper.deleteById(n.getId());   // 逻辑删除
    }

    public Long resolveSpaceId(String refType, Long refId) {
        SpaceNode n = findByRef(refType, refId);
        return n == null ? null : n.getId();
    }

    private void syncProject(Project p) {
        if (p == null) return;
        upsert("project", p.getId(), null, "PROJECT", SpaceCodec.PREFIX_PROJECT, p.getCode(), p.getName());
    }

    private void syncBuilding(Building b) {
        if (b == null) return;
        SpaceNode parent = findByRef("project", b.getProjectId());
        upsert("building", b.getId(), parent, "BUILDING", SpaceCodec.PREFIX_BUILDING, b.getCode(), b.getName());
    }

    private void syncFloor(Floor f) {
        if (f == null) return;
        SpaceNode parent = findByRef("building", f.getBuildingId());
        upsert("floor", f.getId(), parent, "FLOOR", SpaceCodec.PREFIX_FLOOR, f.getCode(), floorName(f));
    }

    private void syncRoom(Room r) {
        if (r == null) return;
        SpaceNode parent = findByRef("floor", r.getFloorId());
        upsert("room", r.getId(), parent, "ROOM", SpaceCodec.PREFIX_ROOM, r.getCode(), r.getRoomNo());
    }

    private String floorName(Floor f) {
        try {
            Object v = f.getClass().getMethod("getName").invoke(f);
            if (v != null) return v.toString();
        } catch (Exception ignore) {}
        return f.getCode() != null ? f.getCode() : ("F#" + f.getId());
    }

    private void upsert(String refType, Long refId, SpaceNode parent,
                        String type, String prefix, String rawCode, String name) {
        SpaceNode existing = findByRef(refType, refId);
        String parentCode = parent == null ? null : parent.getCode();
        String parentPath = parent == null ? null : parent.getPath();
        Long parentId = parent == null ? null : parent.getId();
        int level = parent == null ? 1 : parent.getLevel() + 1;

        SpaceNode node = existing != null ? existing : new SpaceNode();
        node.setParentId(parentId);
        node.setLevel(level);
        node.setType(type);
        node.setCode(SpaceCodec.childCode(parentCode, prefix, rawCode, refId));
        node.setName(name == null ? "" : name);
        node.setRefType(refType);
        node.setRefId(refId);
        if (node.getStatus() == null) node.setStatus(1);
        if (node.getSort() == null) node.setSort(0);

        if (existing == null) {
            spaceMapper.insert(node);
            node.setPath(SpaceCodec.buildPath(parentPath, node.getId()));
            spaceMapper.updateById(node);
        } else {
            node.setPath(SpaceCodec.buildPath(parentPath, node.getId()));
            spaceMapper.updateById(node);
        }
    }

    private SpaceNode findByRef(String refType, Long refId) {
        if (refType == null || refId == null) return null;
        return spaceMapper.selectOne(new LambdaQueryWrapper<SpaceNode>()
                .eq(SpaceNode::getRefType, refType)
                .eq(SpaceNode::getRefId, refId)
                .last("limit 1"));
    }
}
```

- [ ] **Step 3: 容器编译**

Run: `docker run --rm -v "$PWD":/app -v ~/.m2:/root/.m2 -w /app maven:3.9-eclipse-temurin-17 mvn -DskipTests compile`
Expected: BUILD SUCCESS（若 Building/Floor 无 getCode/getProjectId 等，按 Step1 结果调整字段名）

- [ ] **Step 4: Commit**

```bash
git add backend/src/main/java/com/zhyq/park/space/service/SpaceSyncService.java
git commit -m "feat(space): SpaceSyncService 回填/增量/删除/resolveSpaceId"
```

---

### Task 5: SpaceController 只读查询 + 手动 reconcile

**Files:**
- Create: `backend/src/main/java/com/zhyq/park/space/controller/SpaceController.java`

**Interfaces:**
- Consumes: `SpaceNodeMapper`, `SpaceSyncService`, `com.zhyq.park.common.result.Result`。
- Produces: 前缀 `/space` 的接口：`GET /tree`、`GET /children`、`GET /{id}/subtree-ids`、`GET /{id}/path`、`GET /by-ref`、`POST /reconcile`。

- [ ] **Step 1: 确认 Result.ok 签名**

Run: `grep -E "public static.*ok\(|public static.*fail\(" backend/src/main/java/com/zhyq/park/common/result/Result.java`
Expected: 确认 `Result.ok(T)` 与 `Result.ok()` 存在；不同则微调下方调用。

- [ ] **Step 2: 写 SpaceController**

```java
package com.zhyq.park.space.controller;

import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import com.zhyq.park.common.result.Result;
import com.zhyq.park.space.entity.SpaceNode;
import com.zhyq.park.space.mapper.SpaceNodeMapper;
import com.zhyq.park.space.service.SpaceSyncService;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.tags.Tag;
import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.*;

import java.util.ArrayList;
import java.util.List;

@Tag(name = "空间树")
@RestController
@RequestMapping("/space")
@RequiredArgsConstructor
public class SpaceController {

    private final SpaceNodeMapper spaceMapper;
    private final SpaceSyncService syncService;

    @Operation(summary = "完整空间树")
    @GetMapping("/tree")
    public Result<List<SpaceNode>> tree() {
        return Result.ok(spaceMapper.selectList(new LambdaQueryWrapper<SpaceNode>()
                .eq(SpaceNode::getStatus, 1)
                .orderByAsc(SpaceNode::getLevel).orderByAsc(SpaceNode::getSort)));
    }

    @Operation(summary = "子节点")
    @GetMapping("/children")
    public Result<List<SpaceNode>> children(@RequestParam(required = false) Long parentId) {
        LambdaQueryWrapper<SpaceNode> w = new LambdaQueryWrapper<SpaceNode>().orderByAsc(SpaceNode::getSort);
        if (parentId == null) w.isNull(SpaceNode::getParentId); else w.eq(SpaceNode::getParentId, parentId);
        return Result.ok(spaceMapper.selectList(w));
    }

    @Operation(summary = "子树id集合（含自身）")
    @GetMapping("/{id}/subtree-ids")
    public Result<List<Long>> subtreeIds(@PathVariable Long id) {
        SpaceNode self = spaceMapper.selectById(id);
        if (self == null) return Result.ok(new ArrayList<>());
        List<Long> ids = new ArrayList<>();
        for (SpaceNode n : spaceMapper.selectSubtree(self.getPath() + "%")) ids.add(n.getId());
        return Result.ok(ids);
    }

    @Operation(summary = "面包屑路径")
    @GetMapping("/{id}/path")
    public Result<List<SpaceNode>> path(@PathVariable Long id) {
        SpaceNode self = spaceMapper.selectById(id);
        List<SpaceNode> chain = new ArrayList<>();
        if (self == null || self.getPath() == null) return Result.ok(chain);
        for (String seg : self.getPath().split("/")) {
            if (seg.isBlank()) continue;
            SpaceNode n = spaceMapper.selectById(Long.valueOf(seg));
            if (n != null) chain.add(n);
        }
        return Result.ok(chain);
    }

    @Operation(summary = "按旧对象反查空间节点")
    @GetMapping("/by-ref")
    public Result<SpaceNode> byRef(@RequestParam String refType, @RequestParam Long refId) {
        return Result.ok(spaceMapper.selectOne(new LambdaQueryWrapper<SpaceNode>()
                .eq(SpaceNode::getRefType, refType).eq(SpaceNode::getRefId, refId).last("limit 1")));
    }

    @Operation(summary = "全量回填空间树（幂等）")
    @PostMapping("/reconcile")
    public Result<Void> reconcile() {
        syncService.reconcile();
        return Result.ok();
    }
}
```

- [ ] **Step 3: 容器编译**

Run: `docker run --rm -v "$PWD":/app -v ~/.m2:/root/.m2 -w /app maven:3.9-eclipse-temurin-17 mvn -DskipTests compile`
Expected: BUILD SUCCESS

- [ ] **Step 4: Commit**

```bash
git add backend/src/main/java/com/zhyq/park/space/controller/SpaceController.java
git commit -m "feat(space): SpaceController 只读查询 + 手动 reconcile"
```

---

### Task 6: building 四 controller 增量同步 + 夜间对账

**Files:**
- Modify: `backend/src/main/java/com/zhyq/park/building/controller/{Project,Building,Floor,Room}Controller.java`
- Create: `backend/src/main/java/com/zhyq/park/space/service/SpaceReconcileJob.java`
- Modify: 主启动类（若无 `@EnableScheduling` 则加）

**Interfaces:**
- Consumes: `SpaceSyncService`。
- Produces: 各 controller add/update 后调 `sync(refType,id)`、delete 后调 `remove(refType,id)`（try-catch 吞异常）；`SpaceReconcileJob` 每日对账。

- [ ] **Step 1: 读一个 controller 确认方法形态**

Run: `sed -n '1,80p' backend/src/main/java/com/zhyq/park/building/controller/RoomController.java`
Expected: 看清 add/update/delete 方法名、返回类型、保存后主键获取方式（`entity.getId()` 或路径参数 `id`）。四个 controller 同构，以此为模板。

- [ ] **Step 2: RoomController 注入 service + 追加同步（四个 controller 同构，refType 各异）**

注入 final 字段（`@RequiredArgsConstructor` 已在则自动装配）：
```java
private final com.zhyq.park.space.service.SpaceSyncService spaceSyncService;
```
在 add/update 成功返回前追加（`savedId` = 实际主键变量）：
```java
try { spaceSyncService.sync("room", savedId); } catch (Exception e) { log.warn("space sync fail room {}", savedId, e); }
```
在 delete 成功后追加：
```java
try { spaceSyncService.remove("room", id); } catch (Exception e) { log.warn("space remove fail room {}", id, e); }
```
ProjectController 用 `"project"`、BuildingController 用 `"building"`、FloorController 用 `"floor"`，变量名对应调整。**只在末尾追加，不改原增删改逻辑本体。** 若类未引 `@Slf4j`，加 `import lombok.extern.slf4j.Slf4j;` 与类注解，或去掉 log 仅留空 catch。

- [ ] **Step 3: 写夜间对账 Job**

```java
package com.zhyq.park.space.service;

import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.scheduling.annotation.Scheduled;
import org.springframework.stereotype.Component;

@Slf4j
@Component
@RequiredArgsConstructor
public class SpaceReconcileJob {
    private final SpaceSyncService syncService;

    /** 每日 03:17 全量对账，抓增量遗漏/手工改库漂移 */
    @Scheduled(cron = "0 17 3 * * ?")
    public void nightlyReconcile() {
        try { syncService.reconcile(); }
        catch (Exception e) { log.error("[space] nightly reconcile failed", e); }
    }
}
```

- [ ] **Step 4: 确认 @EnableScheduling**

Run: `grep -rn "EnableScheduling\|@SpringBootApplication" backend/src/main/java/com/zhyq/park/*.java`
Expected: 若无 `@EnableScheduling`，在主启动类加。

- [ ] **Step 5: 容器编译**

Run: `docker run --rm -v "$PWD":/app -v ~/.m2:/root/.m2 -w /app maven:3.9-eclipse-temurin-17 mvn -DskipTests compile`
Expected: BUILD SUCCESS

- [ ] **Step 6: Commit**

```bash
git add backend/src/main/java/com/zhyq/park/building/controller/ backend/src/main/java/com/zhyq/park/space/service/SpaceReconcileJob.java backend/src/main/java/com/zhyq/park/*.java
git commit -m "feat(space): building CRUD 增量同步 + 夜间对账 Job"
```

---

### Task 7: 全栈冒烟验证 + 历史回填

**Files:**
- Create（可选）: `backend/src/main/resources/db/migration/V21__backfill_space_id.sql`（历史数据回填，D2 范围确认后写）

- [ ] **Step 1: 起全栈（Flyway 自动执行 V19）**

Run: `open -a Docker`（确保 daemon），项目根 `docker compose -f docker-compose.full.yml up -d --build`
Expected: 三容器起，`http://localhost` 可访问；后端日志显示 Flyway 迁移到 V19（或 V20/V21 若已含 #7）。

- [ ] **Step 2: 登录拿 token**

Run:
```bash
curl -s -X POST http://localhost/api/auth/login -H 'Content-Type: application/json' -d '{"username":"admin","password":"zhyq@2026"}'
```
Expected: 返回含 `token` 的 Result。记为 `TK`。

- [ ] **Step 3: 触发回填并验证树**

Run:
```bash
curl -s -X POST http://localhost/api/space/reconcile -H "Authorization: Bearer $TK"
curl -s http://localhost/api/space/tree -H "Authorization: Bearer $TK"
```
Expected: reconcile 返回 ok；tree 返回节点，type 覆盖四类，code 形如 `P..-B..-F..-R..`，path 形如 `/1/5/23/`。

- [ ] **Step 4: 验证子树聚合与面包屑**

取一个 building 节点 id（BID）：
```bash
curl -s http://localhost/api/space/<BID>/subtree-ids -H "Authorization: Bearer $TK"
curl -s http://localhost/api/space/<BID>/path -H "Authorization: Bearer $TK"
```
Expected: subtree-ids 含该楼下所有 floor/room id；path 返回 项目→楼栋 链。

- [ ] **Step 5: 零触碰确认（既有功能）**

前端登录，打开 楼宇管理 / 租控图 / 合同列表，确认房态、列表、锁房源等既有页面正常（业务表加列不影响既有查询）。

- [ ] **Step 6: 验证增量同步**

前端或 API 新建一个房间 → `curl -s "http://localhost/api/space/by-ref?refType=room&refId=<新id>" -H "Authorization: Bearer $TK"` → 返回对应新空间节点。

- [ ] **Step 7: 历史业务数据回填（D2 范围确认后）**

写 `V21__backfill_space_id.sql`（对每张已加列的业务表，roomId 列名以实际为准）：
```sql
UPDATE biz_contract c JOIN sys_space s ON s.ref_type='room' AND s.ref_id=c.room_id AND s.deleted=0 SET c.space_id=s.id WHERE c.room_id IS NOT NULL;
UPDATE pm_work_order w JOIN sys_space s ON s.ref_type='room' AND s.ref_id=w.room_id AND s.deleted=0 SET w.space_id=s.id WHERE w.room_id IS NOT NULL;
UPDATE eng_reading  r JOIN eng_meter m ON r.meter_id=m.id JOIN sys_space s ON s.ref_type='room' AND s.ref_id=m.room_id AND s.deleted=0 SET r.space_id=s.id;
UPDATE iot_device   d JOIN sys_space s ON s.ref_type='room' AND s.ref_id=d.room_id AND s.deleted=0 SET d.space_id=s.id WHERE d.room_id IS NOT NULL;
-- biz_contract_room 逐房源: JOIN 其 room_id 列
```
> ⚠️ 各表 roomId / meterId 的**真实列名需在编码时用 `SHOW COLUMNS` 或读建表 SQL 确认**再定稿。回填依赖 sys_space 已 reconcile（room 节点存在）。
Run: 重启后端令 V21 执行，抽查 `SELECT space_id FROM biz_contract WHERE room_id IS NOT NULL LIMIT 5`。
Expected: space_id 已填。

```bash
git add backend/src/main/resources/db/migration/V21__backfill_space_id.sql
git commit -m "feat(space): V21 历史业务数据回填 space_id"
```

---

## Self-Review

- **Spec 覆盖**：sys_space 表 + 业务表加列(Task1)；实体/Mapper(Task2)；编码/path 纯函数(Task3)；回填/增量/删除/resolveSpaceId(Task4)；只读 API + 手动 reconcile(Task5)；building 接入 + 夜间对账(Task6)；冒烟 + 历史回填(Task7)。设计文档 §3~§6 全部有对应任务。
- **占位符扫描**：无 TBD/TODO。Task7 Step7 的 roomId 真实列名标注为"编码时确认"——这是**主动风险标注**（各业务表 roomId 列名未逐一核实），非空泛占位；给出了确认手段（SHOW COLUMNS/读建表 SQL）。
- **类型一致**：`sync/remove/resolveSpaceId/reconcile` 命名跨 Task4→5→6 一致；`selectSubtree(pathPrefix)` Task2 定义、Task5 使用一致；`SpaceNode` 字段跨 Task2→4→5 一致。
- **风险备注**：Building/Floor 实体的 getCode/getProjectId/getBuildingId 等 getter 由 Task4 Step1 与 Step3 编译确认；Floor.name 用反射兜底可在确认后简化。
