# #7 鉴权补强 实施计划（Spring Security + BCrypt + 无状态 JWT）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把演示级鉴权（SHA-256 + 内存 token + WebMvc 拦截器）升级为生产可用鉴权：BCrypt 密码、Spring Security 过滤链、无状态双 JWT、角色级授权，并把审计操作人从固定 "system" 换成真实登录用户。

**Architecture:** Spring Security `SecurityFilterChain`（STATELESS）+ `JwtAuthenticationFilter` 从 access token 的 `roles` claim 装配权限；登录走 `AuthenticationManager` + `UserDetailsService`（查 sys_user + sys_user_role→sys_role）。access(2h)/refresh(7d) 均无状态、无服务端存储。旧密码经 Flyway V20 一次性重置为 BCrypt。删除旧 TokenStore/AuthInterceptorConfig。

**Tech Stack:** Spring Boot 3.2 + Spring Security + jjwt 0.12.x + MyBatis-Plus + Flyway；无本机 JDK，编译走 maven Docker 容器。

## Global Constraints

- 后端包结构：安全组件放 `com.zhyq.park.common.security`；补的 mapper 放 `com.zhyq.park.system.mapper`（**必须 `*.mapper` 结尾包**，@MapperScan 运行期扫描）。
- 401/403 响应体**必须保持** `Result` 形状 `{code,msg,data}`（前端 axios 拦截器不改）。现有 401 文案："未登录或登录已过期"。
- `sys_user.password` 有 `@JsonProperty(access=WRITE_ONLY)`，勿破坏。
- JWT 密钥走环境变量占位 `JWT_SECRET`（与项目 `DB_URL/DB_USERNAME/DB_PASSWORD` 占位风格一致），本地默认值保留。
- Flyway 当前最新 **V18**；本计划密码重置用 **V20**（#3 计划占用 V19，若两计划并行需协调版本号，本计划假定 #3 先行占 V19）。
- 编译：`docker run --rm -v "$PWD":/app -v ~/.m2:/root/.m2 -w /app maven:3.9-eclipse-temurin-17 mvn -DskipTests compile`（backend/ 下）。
- 全栈冒烟：`docker compose -f docker-compose.full.yml up -d --build`，`http://localhost`，后端 context-path `/api`。
- **⚠️ 密码重置（V20）不可回退**——上线前须确认。admin 保持 `zhyq@2026` 可登录。
- 无单元测试框架；JWT 签发/解析纯逻辑用 maven 容器内 JUnit，其余靠编译 + localhost 实登冒烟。

---

### Task 1: 加依赖 + 探明现状锚点

**Files:**
- Modify: `backend/pom.xml`

**Interfaces:**
- Produces: pom 含 `spring-boot-starter-security`、`jjwt-api/impl/jackson`、`spring-boot-starter-test`(test)。

- [ ] **Step 1: 确认现有种子角色码（授权标注要用）**

Run: `grep -iE "insert into sys_role|sys_user_role|'ADMIN'|role" backend/src/main/resources/db/migration/V8__seed.sql`
Expected: 记下 sys_role 种子的 `code` 值（如 ADMIN / OPERATOR / …）与 admin 绑定的 role_id。授权注解 `hasRole('X')` 的 X 必须对齐这里。

- [ ] **Step 2: 确认主启动类与 pom 结构**

Run: `grep -rn "@SpringBootApplication" backend/src/main/java; grep -n "spring-boot-starter-web\|spring-boot-starter-test" backend/pom.xml`
Expected: 定位主类全路径；确认 web starter 已在、test starter 是否已在。

- [ ] **Step 3: 加依赖到 pom.xml**

在 `<dependencies>` 内加（jjwt 0.12.x）：
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-security</artifactId>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-api</artifactId>
    <version>0.12.6</version>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-impl</artifactId>
    <version>0.12.6</version>
    <scope>runtime</scope>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-jackson</artifactId>
    <version>0.12.6</version>
    <scope>runtime</scope>
</dependency>
<!-- 若 Step2 显示 test starter 未在，再加： -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-test</artifactId>
    <scope>test</scope>
</dependency>
```

- [ ] **Step 4: 容器编译拉依赖**

Run: `docker run --rm -v "$PWD":/app -v ~/.m2:/root/.m2 -w /app maven:3.9-eclipse-temurin-17 mvn -DskipTests compile`
Expected: BUILD SUCCESS。**⚠️ 此时 Security 已进 classpath，默认给全端点加 basic auth——在 Task5 的 SecurityConfig 落地前不要起服务，避免默认拦截误判。**

- [ ] **Step 5: Commit**

```bash
git add backend/pom.xml
git commit -m "chore(auth): 引入 spring-security + jjwt 依赖"
```

---

### Task 2: JwtService + 单测

**Files:**
- Create: `backend/src/main/java/com/zhyq/park/common/security/JwtService.java`
- Modify: `backend/src/main/resources/application.yml`（加 `jwt` 段）
- Test: `backend/src/test/java/com/zhyq/park/common/security/JwtServiceTest.java`

**Interfaces:**
- Produces: `JwtService`：
  - `String generateAccess(Long uid, String username, List<String> roles)`
  - `String generateRefresh(Long uid, String username)`
  - `Claims parse(String token)`（验签+过期，失败抛 `JwtException`）
  - `boolean isRefresh(Claims claims)`
  - `void init(String secret, long accessTtlMinutes, long refreshTtlDays)`（供测试注入）
  - claims key：`uid`、`roles`(List<String>)、`type`（"refresh" 仅 refresh 有）。

- [ ] **Step 1: application.yml 加 jwt 段**

在 `backend/src/main/resources/application.yml` 顶层加：
```yaml
jwt:
  secret: ${JWT_SECRET:zhyq-dev-secret-please-change-in-prod-0123456789abcdef}
  access-ttl-minutes: 120
  refresh-ttl-days: 7
```
（secret 需 ≥ 32 字节供 HS256；默认值仅本地用。）

- [ ] **Step 2: 写失败测试**

```java
package com.zhyq.park.common.security;

import io.jsonwebtoken.Claims;
import org.junit.jupiter.api.Test;
import java.util.List;
import static org.junit.jupiter.api.Assertions.*;

class JwtServiceTest {

    private JwtService svc() {
        JwtService s = new JwtService();
        s.init("test-secret-test-secret-test-secret-0123456789", 120, 7);
        return s;
    }

    @Test
    void accessToken_roundtrips_uidAndRoles() {
        JwtService s = svc();
        String token = s.generateAccess(7L, "admin", List.of("ADMIN", "OPERATOR"));
        Claims c = s.parse(token);
        assertEquals("admin", c.getSubject());
        assertEquals(7, ((Number) c.get("uid")).intValue());
        assertEquals(List.of("ADMIN", "OPERATOR"), c.get("roles"));
        assertFalse(s.isRefresh(c));
    }

    @Test
    void refreshToken_isMarkedRefresh() {
        JwtService s = svc();
        Claims c = s.parse(s.generateRefresh(7L, "admin"));
        assertTrue(s.isRefresh(c));
    }

    @Test
    void tamperedToken_throws() {
        JwtService s = svc();
        String token = s.generateAccess(7L, "admin", List.of("ADMIN"));
        assertThrows(Exception.class, () -> s.parse(token + "x"));
    }
}
```

- [ ] **Step 3: 运行确认失败**

Run: `docker run --rm -v "$PWD":/app -v ~/.m2:/root/.m2 -w /app maven:3.9-eclipse-temurin-17 mvn test -Dtest=JwtServiceTest`
Expected: FAIL（JwtService 不存在）

- [ ] **Step 4: 写 JwtService**

```java
package com.zhyq.park.common.security;

import io.jsonwebtoken.Claims;
import io.jsonwebtoken.Jwts;
import io.jsonwebtoken.security.Keys;
import jakarta.annotation.PostConstruct;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;

import javax.crypto.SecretKey;
import java.nio.charset.StandardCharsets;
import java.util.Date;
import java.util.List;
import java.util.Map;

@Component
public class JwtService {

    @Value("${jwt.secret:}")
    private String secret;
    @Value("${jwt.access-ttl-minutes:120}")
    private long accessTtlMinutes;
    @Value("${jwt.refresh-ttl-days:7}")
    private long refreshTtlDays;

    private SecretKey key;

    @PostConstruct
    public void initFromConfig() { init(secret, accessTtlMinutes, refreshTtlDays); }

    /** 供测试直接注入 */
    public void init(String secret, long accessTtlMinutes, long refreshTtlDays) {
        this.secret = secret;
        this.accessTtlMinutes = accessTtlMinutes;
        this.refreshTtlDays = refreshTtlDays;
        this.key = Keys.hmacShaKeyFor(secret.getBytes(StandardCharsets.UTF_8));
    }

    public String generateAccess(Long uid, String username, List<String> roles) {
        long now = System.currentTimeMillis();
        return Jwts.builder()
                .subject(username)
                .claims(Map.of("uid", uid, "roles", roles))
                .issuedAt(new Date(now))
                .expiration(new Date(now + accessTtlMinutes * 60_000))
                .signWith(key)
                .compact();
    }

    public String generateRefresh(Long uid, String username) {
        long now = System.currentTimeMillis();
        return Jwts.builder()
                .subject(username)
                .claims(Map.of("uid", uid, "type", "refresh"))
                .issuedAt(new Date(now))
                .expiration(new Date(now + refreshTtlDays * 86_400_000L))
                .signWith(key)
                .compact();
    }

    public Claims parse(String token) {
        return Jwts.parser().verifyWith(key).build().parseSignedClaims(token).getPayload();
    }

    public boolean isRefresh(Claims claims) {
        return "refresh".equals(claims.get("type"));
    }
}
```

- [ ] **Step 5: 运行确认通过**

Run: `docker run --rm -v "$PWD":/app -v ~/.m2:/root/.m2 -w /app maven:3.9-eclipse-temurin-17 mvn test -Dtest=JwtServiceTest`
Expected: PASS（3 tests）

- [ ] **Step 6: Commit**

```bash
git add backend/src/main/java/com/zhyq/park/common/security/JwtService.java backend/src/main/resources/application.yml backend/src/test/java/com/zhyq/park/common/security/JwtServiceTest.java
git commit -m "feat(auth): JwtService 双令牌签发/解析（含单测）"
```

---

### Task 3: SysUserRole 查询 + UserDetailsServiceImpl

**Files:**
- Create: `backend/src/main/java/com/zhyq/park/system/mapper/SysUserRoleMapper.java`
- Create: `backend/src/main/java/com/zhyq/park/common/security/LoginUser.java`
- Create: `backend/src/main/java/com/zhyq/park/common/security/UserDetailsServiceImpl.java`

**Interfaces:**
- Consumes: `SysUserMapper`（既有）、`SysUser`（含 id/username/password/status）。
- Produces:
  - `SysUserRoleMapper.selectRoleCodesByUserId(Long userId)` → `List<String>`。
  - `LoginUser implements UserDetails`（uid、username、password、roles；getAuthorities 映射 `ROLE_+code`）。
  - `UserDetailsServiceImpl implements UserDetailsService`。

- [ ] **Step 1: 核实 sys_role 列（deleted/status 是否存在）**

Run: `grep -A15 "CREATE TABLE sys_role " backend/src/main/resources/db/migration/V1__system.sql`
Expected: 确认 sys_role 有 `code`、`status`、`deleted` 列。若无 deleted，去掉下方 SQL 的 `r.deleted=0`。

- [ ] **Step 2: 写 SysUserRoleMapper（放 *.mapper 包）**

```java
package com.zhyq.park.system.mapper;

import org.apache.ibatis.annotations.Param;
import org.apache.ibatis.annotations.Select;
import java.util.List;

public interface SysUserRoleMapper {

    @Select("SELECT r.code FROM sys_user_role ur JOIN sys_role r ON ur.role_id = r.id " +
            "WHERE ur.user_id = #{userId} AND r.status = 1 AND r.deleted = 0")
    List<String> selectRoleCodesByUserId(@Param("userId") Long userId);
}
```

- [ ] **Step 3: 写 LoginUser**

```java
package com.zhyq.park.common.security;

import lombok.Getter;
import org.springframework.security.core.GrantedAuthority;
import org.springframework.security.core.authority.SimpleGrantedAuthority;
import org.springframework.security.core.userdetails.UserDetails;

import java.util.Collection;
import java.util.List;

@Getter
public class LoginUser implements UserDetails {
    private final Long uid;
    private final String username;
    private final String password;
    private final List<String> roles;

    public LoginUser(Long uid, String username, String password, List<String> roles) {
        this.uid = uid; this.username = username; this.password = password; this.roles = roles;
    }

    @Override public Collection<? extends GrantedAuthority> getAuthorities() {
        return roles.stream().map(r -> (GrantedAuthority) new SimpleGrantedAuthority("ROLE_" + r)).toList();
    }
    @Override public String getPassword() { return password; }
    @Override public String getUsername() { return username; }
    @Override public boolean isAccountNonExpired() { return true; }
    @Override public boolean isAccountNonLocked() { return true; }
    @Override public boolean isCredentialsNonExpired() { return true; }
    @Override public boolean isEnabled() { return true; }
}
```

- [ ] **Step 4: 写 UserDetailsServiceImpl**

```java
package com.zhyq.park.common.security;

import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import com.zhyq.park.system.entity.SysUser;
import com.zhyq.park.system.mapper.SysUserMapper;
import com.zhyq.park.system.mapper.SysUserRoleMapper;
import lombok.RequiredArgsConstructor;
import org.springframework.security.core.userdetails.*;
import org.springframework.stereotype.Service;

import java.util.List;

@Service
@RequiredArgsConstructor
public class UserDetailsServiceImpl implements UserDetailsService {

    private final SysUserMapper userMapper;
    private final SysUserRoleMapper userRoleMapper;

    @Override
    public UserDetails loadUserByUsername(String username) throws UsernameNotFoundException {
        SysUser u = userMapper.selectOne(new LambdaQueryWrapper<SysUser>()
                .eq(SysUser::getUsername, username).last("limit 1"));
        if (u == null || u.getStatus() == null || u.getStatus() != 1) {
            throw new UsernameNotFoundException("账号不存在或已停用");
        }
        List<String> roles = userRoleMapper.selectRoleCodesByUserId(u.getId());
        return new LoginUser(u.getId(), u.getUsername(), u.getPassword(), roles);
    }
}
```

- [ ] **Step 5: 容器编译**

Run: `docker run --rm -v "$PWD":/app -v ~/.m2:/root/.m2 -w /app maven:3.9-eclipse-temurin-17 mvn -DskipTests compile`
Expected: BUILD SUCCESS

- [ ] **Step 6: Commit**

```bash
git add backend/src/main/java/com/zhyq/park/system/mapper/SysUserRoleMapper.java backend/src/main/java/com/zhyq/park/common/security/LoginUser.java backend/src/main/java/com/zhyq/park/common/security/UserDetailsServiceImpl.java
git commit -m "feat(auth): 角色码联表查询 + LoginUser + UserDetailsService"
```

---

### Task 4: JwtAuthenticationFilter + 401/403 处理器

**Files:**
- Create: `backend/src/main/java/com/zhyq/park/common/security/JwtAuthenticationFilter.java`
- Create: `backend/src/main/java/com/zhyq/park/common/security/RestAuthEntryPoint.java`
- Create: `backend/src/main/java/com/zhyq/park/common/security/RestAccessDeniedHandler.java`

**Interfaces:**
- Consumes: `JwtService`, `Result`, `LoginUser`。
- Produces: `JwtAuthenticationFilter extends OncePerRequestFilter`；`RestAuthEntryPoint`(401)；`RestAccessDeniedHandler`(403)，均输出 Result 形状。

- [ ] **Step 1: 写 JwtAuthenticationFilter**

```java
package com.zhyq.park.common.security;

import io.jsonwebtoken.Claims;
import jakarta.servlet.FilterChain;
import jakarta.servlet.ServletException;
import jakarta.servlet.http.*;
import lombok.RequiredArgsConstructor;
import org.springframework.lang.NonNull;
import org.springframework.security.authentication.UsernamePasswordAuthenticationToken;
import org.springframework.security.core.authority.SimpleGrantedAuthority;
import org.springframework.security.core.context.SecurityContextHolder;
import org.springframework.stereotype.Component;
import org.springframework.web.filter.OncePerRequestFilter;

import java.io.IOException;
import java.util.List;

@Component
@RequiredArgsConstructor
public class JwtAuthenticationFilter extends OncePerRequestFilter {

    private final JwtService jwtService;

    @Override
    protected void doFilterInternal(@NonNull HttpServletRequest req, @NonNull HttpServletResponse res,
                                    @NonNull FilterChain chain) throws ServletException, IOException {
        String auth = req.getHeader("Authorization");
        if (auth != null && auth.startsWith("Bearer ")) {
            try {
                Claims c = jwtService.parse(auth.substring(7));
                if (!jwtService.isRefresh(c)) {   // refresh 不能当访问令牌
                    @SuppressWarnings("unchecked")
                    List<String> roles = (List<String>) c.getOrDefault("roles", List.of());
                    List<SimpleGrantedAuthority> authorities =
                            roles.stream().map(r -> new SimpleGrantedAuthority("ROLE_" + r)).toList();
                    LoginUser principal = new LoginUser(
                            ((Number) c.get("uid")).longValue(), c.getSubject(), null, roles);
                    var token = new UsernamePasswordAuthenticationToken(principal, null, authorities);
                    SecurityContextHolder.getContext().setAuthentication(token);
                }
            } catch (Exception ignore) {
                SecurityContextHolder.clearContext();
            }
        }
        chain.doFilter(req, res);
    }
}
```

- [ ] **Step 2: 写 401 EntryPoint**

```java
package com.zhyq.park.common.security;

import com.fasterxml.jackson.databind.ObjectMapper;
import com.zhyq.park.common.result.Result;
import jakarta.servlet.http.*;
import org.springframework.security.core.AuthenticationException;
import org.springframework.security.web.AuthenticationEntryPoint;
import org.springframework.stereotype.Component;

import java.io.IOException;

@Component
public class RestAuthEntryPoint implements AuthenticationEntryPoint {
    private final ObjectMapper om = new ObjectMapper();
    @Override
    public void commence(HttpServletRequest req, HttpServletResponse res, AuthenticationException e) throws IOException {
        res.setStatus(401);
        res.setContentType("application/json;charset=UTF-8");
        res.getWriter().write(om.writeValueAsString(Result.fail(401, "未登录或登录已过期")));
    }
}
```

- [ ] **Step 3: 写 403 Handler**

```java
package com.zhyq.park.common.security;

import com.fasterxml.jackson.databind.ObjectMapper;
import com.zhyq.park.common.result.Result;
import jakarta.servlet.http.*;
import org.springframework.security.access.AccessDeniedException;
import org.springframework.security.web.access.AccessDeniedHandler;
import org.springframework.stereotype.Component;

import java.io.IOException;

@Component
public class RestAccessDeniedHandler implements AccessDeniedHandler {
    private final ObjectMapper om = new ObjectMapper();
    @Override
    public void handle(HttpServletRequest req, HttpServletResponse res, AccessDeniedException e) throws IOException {
        res.setStatus(403);
        res.setContentType("application/json;charset=UTF-8");
        res.getWriter().write(om.writeValueAsString(Result.fail(403, "无权限")));
    }
}
```

- [ ] **Step 4: 确认 Result.fail 签名**

Run: `grep -E "public static.*fail\(" backend/src/main/java/com/zhyq/park/common/result/Result.java`
Expected: 存在 `Result.fail(int, String)`；不同则微调。

- [ ] **Step 5: 容器编译**

Run: `docker run --rm -v "$PWD":/app -v ~/.m2:/root/.m2 -w /app maven:3.9-eclipse-temurin-17 mvn -DskipTests compile`
Expected: BUILD SUCCESS

- [ ] **Step 6: Commit**

```bash
git add backend/src/main/java/com/zhyq/park/common/security/JwtAuthenticationFilter.java backend/src/main/java/com/zhyq/park/common/security/RestAuthEntryPoint.java backend/src/main/java/com/zhyq/park/common/security/RestAccessDeniedHandler.java
git commit -m "feat(auth): JWT 过滤器 + 401/403 处理器（Result 形状）"
```

---

### Task 5: SecurityConfig + 删除旧鉴权 + 重写 AuthController

**Files:**
- Create: `backend/src/main/java/com/zhyq/park/common/security/SecurityConfig.java`
- Create: `backend/src/main/java/com/zhyq/park/common/security/SecurityUtils.java`
- Delete: `backend/src/main/java/com/zhyq/park/auth/TokenStore.java`
- Delete: `backend/src/main/java/com/zhyq/park/auth/AuthInterceptorConfig.java`
- Modify(rewrite): `backend/src/main/java/com/zhyq/park/auth/AuthController.java`

**Interfaces:**
- Consumes: `JwtService`, `UserDetailsServiceImpl`, `JwtAuthenticationFilter`, `RestAuthEntryPoint`, `RestAccessDeniedHandler`。
- Produces: `SecurityConfig`（SecurityFilterChain + PasswordEncoder + AuthenticationManager）；`SecurityUtils.currentUsername()/currentUid()/current()`；AuthController `/auth/login|refresh|logout|me`。

- [ ] **Step 1: 写 SecurityConfig**

```java
package com.zhyq.park.common.security;

import lombok.RequiredArgsConstructor;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.authentication.AuthenticationManager;
import org.springframework.security.config.annotation.authentication.configuration.AuthenticationConfiguration;
import org.springframework.security.config.annotation.method.configuration.EnableMethodSecurity;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.config.annotation.web.configurers.AbstractHttpConfigurer;
import org.springframework.security.config.http.SessionCreationPolicy;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.security.web.SecurityFilterChain;
import org.springframework.security.web.authentication.UsernamePasswordAuthenticationFilter;

@Configuration
@EnableMethodSecurity
@RequiredArgsConstructor
public class SecurityConfig {

    private final JwtAuthenticationFilter jwtFilter;
    private final RestAuthEntryPoint entryPoint;
    private final RestAccessDeniedHandler deniedHandler;

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .csrf(AbstractHttpConfigurer::disable)
            .sessionManagement(s -> s.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
            .authorizeHttpRequests(a -> a
                .requestMatchers(
                    "/auth/login", "/auth/refresh",
                    "/doc.html", "/webjars/**", "/v3/api-docs/**", "/swagger-ui/**",
                    "/favicon.ico", "/error"
                ).permitAll()
                .anyRequest().authenticated())
            .exceptionHandling(e -> e.authenticationEntryPoint(entryPoint).accessDeniedHandler(deniedHandler))
            .addFilterBefore(jwtFilter, UsernamePasswordAuthenticationFilter.class);
        return http.build();
    }

    @Bean
    public PasswordEncoder passwordEncoder() { return new BCryptPasswordEncoder(); }

    @Bean
    public AuthenticationManager authenticationManager(AuthenticationConfiguration cfg) throws Exception {
        return cfg.getAuthenticationManager();
    }
}
```
> ⚠️ Spring Security 匹配 servlet path（已去 context-path `/api`），故 permitAll 写 `/auth/login` 而非 `/api/auth/login`。若 Task7 冒烟发现被拦，改带 `/api` 前缀重试——以实登为准。

- [ ] **Step 2: 写 SecurityUtils**

```java
package com.zhyq.park.common.security;

import org.springframework.security.core.Authentication;
import org.springframework.security.core.context.SecurityContextHolder;

public final class SecurityUtils {
    private SecurityUtils() {}

    public static LoginUser current() {
        Authentication a = SecurityContextHolder.getContext().getAuthentication();
        if (a != null && a.getPrincipal() instanceof LoginUser lu) return lu;
        return null;
    }
    public static String currentUsername() {
        LoginUser lu = current();
        return lu != null ? lu.getUsername() : null;
    }
    public static Long currentUid() {
        LoginUser lu = current();
        return lu != null ? lu.getUid() : null;
    }
}
```

- [ ] **Step 3: 删除旧鉴权两文件**

Run: `git rm backend/src/main/java/com/zhyq/park/auth/TokenStore.java backend/src/main/java/com/zhyq/park/auth/AuthInterceptorConfig.java`
Expected: 两文件被删除并暂存。

- [ ] **Step 4: 重写 AuthController（含 refresh 重查角色）**

```java
package com.zhyq.park.auth;

import com.zhyq.park.common.result.Result;
import com.zhyq.park.common.security.JwtService;
import com.zhyq.park.common.security.LoginUser;
import com.zhyq.park.common.security.SecurityUtils;
import com.zhyq.park.common.security.UserDetailsServiceImpl;
import io.jsonwebtoken.Claims;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.tags.Tag;
import lombok.RequiredArgsConstructor;
import org.springframework.security.authentication.*;
import org.springframework.security.core.Authentication;
import org.springframework.web.bind.annotation.*;

import java.util.HashMap;
import java.util.Map;

@Tag(name = "认证")
@RestController
@RequestMapping("/auth")
@RequiredArgsConstructor
public class AuthController {

    private final AuthenticationManager authManager;
    private final JwtService jwtService;
    private final UserDetailsServiceImpl uds;

    @Operation(summary = "登录")
    @PostMapping("/login")
    public Result<Map<String, Object>> login(@RequestBody Map<String, String> body) {
        String username = body.get("username");
        String password = body.get("password");
        Authentication auth;
        try {
            auth = authManager.authenticate(new UsernamePasswordAuthenticationToken(username, password));
        } catch (AuthenticationException e) {
            return Result.fail(401, "账号或密码错误");
        }
        LoginUser u = (LoginUser) auth.getPrincipal();
        Map<String, Object> data = new HashMap<>();
        data.put("token", jwtService.generateAccess(u.getUid(), u.getUsername(), u.getRoles()));
        data.put("refreshToken", jwtService.generateRefresh(u.getUid(), u.getUsername()));
        data.put("username", u.getUsername());
        data.put("roles", u.getRoles());
        return Result.ok(data);
    }

    @Operation(summary = "刷新令牌")
    @PostMapping("/refresh")
    public Result<Map<String, Object>> refresh(@RequestBody Map<String, String> body) {
        try {
            Claims c = jwtService.parse(body.get("refreshToken"));
            if (!jwtService.isRefresh(c)) return Result.fail(401, "无效的刷新令牌");
            LoginUser lu = (LoginUser) uds.loadUserByUsername(c.getSubject());  // 重查角色
            Map<String, Object> data = new HashMap<>();
            data.put("token", jwtService.generateAccess(lu.getUid(), lu.getUsername(), lu.getRoles()));
            return Result.ok(data);
        } catch (Exception e) {
            return Result.fail(401, "刷新令牌无效或已过期");
        }
    }

    @Operation(summary = "登出（无状态，前端弃 token）")
    @PostMapping("/logout")
    public Result<Void> logout() { return Result.ok(); }

    @Operation(summary = "当前用户")
    @GetMapping("/me")
    public Result<Map<String, Object>> me() {
        LoginUser u = SecurityUtils.current();
        if (u == null) return Result.fail(401, "未登录");
        Map<String, Object> data = new HashMap<>();
        data.put("uid", u.getUid());
        data.put("username", u.getUsername());
        data.put("roles", u.getRoles());
        return Result.ok(data);
    }
}
```

- [ ] **Step 5: 容器编译**

Run: `docker run --rm -v "$PWD":/app -v ~/.m2:/root/.m2 -w /app maven:3.9-eclipse-temurin-17 mvn -DskipTests compile`
Expected: BUILD SUCCESS（确认无对已删 TokenStore/AuthInterceptorConfig 的残留引用）

- [ ] **Step 6: Commit**

```bash
git add -A backend/src/main/java/com/zhyq/park/common/security/ backend/src/main/java/com/zhyq/park/auth/
git commit -m "feat(auth): SecurityConfig + SecurityUtils + 重写 AuthController，删除内存 token 鉴权"
```

---

### Task 6: 密码迁移 V20 + 审计真实操作人 + 授权标注

**Files:**
- Create: `backend/src/main/resources/db/migration/V20__auth_bcrypt_reset.sql`
- Modify: `backend/src/main/java/com/zhyq/park/common/config/MyMetaObjectHandler.java`
- Modify: `backend/src/main/java/com/zhyq/park/common/audit/AuditAspect.java`（批次② 建）
- Modify: 高危 controller（合同/财务/系统用户角色）加 `@PreAuthorize`

**Interfaces:**
- Consumes: `SecurityUtils.currentUsername()`。
- Produces: V20 重置密码为 BCrypt；审计/自动填充操作人取真实用户；高危端点角色校验。

- [ ] **Step 1: 离线算 BCrypt 哈希**

在项目内写临时类 `TmpHash.java`（main 方法）打印哈希，用容器跑一次：
```java
// backend/src/main/java/com/zhyq/park/TmpHash.java  （拿到哈希后删除）
public class TmpHash {
    public static void main(String[] a) {
        var enc = new org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder();
        System.out.println("admin=" + enc.encode("zhyq@2026"));
        System.out.println("others=" + enc.encode("Zhyq@2026"));
    }
}
```
Run: `docker run --rm -v "$PWD":/app -v ~/.m2:/root/.m2 -w /app maven:3.9-eclipse-temurin-17 mvn -q compile exec:java -Dexec.mainClass=com.zhyq.park.TmpHash`
Expected: 打印两串 `$2a$10$...`。抄下，删除 TmpHash.java。（exec 插件若无，改用 `mvn test` 里临时打印，或 jshell。哈希每次不同但都可验证，取任一。）

- [ ] **Step 2: 写 V20（填入真实哈希）**

```sql
-- V20__auth_bcrypt_reset.sql  ⚠️ 不可回退：SHA-256 密码重置为 BCrypt
-- admin 保持 zhyq@2026 可登录；其余种子用户重置为 Zhyq@2026（首登须改密）
UPDATE sys_user SET password = '<BCRYPT_HASH_OF_zhyq@2026>' WHERE username = 'admin';
UPDATE sys_user SET password = '<BCRYPT_HASH_OF_Zhyq@2026>' WHERE username <> 'admin';
```
> `<...>` 必须替换为 Step1 生成的实际串再提交。

- [ ] **Step 3: 审计操作人改真实用户**

`MyMetaObjectHandler` 中 create_by/update_by 原填 "system" 处改为：
```java
String operator = com.zhyq.park.common.security.SecurityUtils.currentUsername();
if (operator == null) operator = "system";   // 系统任务/未登录兜底
```
`AuditAspect`（批次②）操作人同样改为 `SecurityUtils.currentUsername()`，null 兜底 "system"。
> 先 `sed -n '1,60p' backend/src/main/java/com/zhyq/park/common/config/MyMetaObjectHandler.java` 看清原填充位置再改。

- [ ] **Step 4: 高危端点加 @PreAuthorize（角色码用 Task1 Step1 确认值，示例 ADMIN）**

- `ContractController` 的 approve、terminate：`@PreAuthorize("hasRole('ADMIN')")`
- finance 收款/发票关键写方法：同上
- `SysUserController` / `SysRoleController` 增删改：`@PreAuthorize("hasRole('ADMIN')")`
> 仅示范，不铺全量。角色码须对齐种子实际值。

- [ ] **Step 5: 容器编译**

Run: `docker run --rm -v "$PWD":/app -v ~/.m2:/root/.m2 -w /app maven:3.9-eclipse-temurin-17 mvn -DskipTests compile`
Expected: BUILD SUCCESS（确认 TmpHash.java 已删，无编译残留）

- [ ] **Step 6: Commit**

```bash
git add -A backend/src/main/resources/db/migration/V20__auth_bcrypt_reset.sql backend/src/main/java/com/zhyq/park/common/ backend/src/main/java/com/zhyq/park/contract/ backend/src/main/java/com/zhyq/park/finance/ backend/src/main/java/com/zhyq/park/system/
git commit -m "feat(auth): V20 密码 BCrypt 重置 + 审计真实操作人 + 高危端点角色校验"
```

---

### Task 7: 全栈冒烟验证（登录/鉴权/刷新/授权）

**Files:** 无（验证任务）

- [ ] **Step 1: 起全栈（Flyway 执行 V20，重置密码）**

Run: `open -a Docker`，项目根 `docker compose -f docker-compose.full.yml up -d --build`
Expected: 后端启动无 502，日志显示 Flyway 到 V20。**若 502：查后端日志——常见坑 SysUserRoleMapper 未被扫（确认 `*.mapper` 包）、permitAll 路径含/不含 context-path。**

- [ ] **Step 2: admin 原口令仍可登录**

Run:
```bash
curl -s -X POST http://localhost/api/auth/login -H 'Content-Type: application/json' -d '{"username":"admin","password":"zhyq@2026"}'
```
Expected: 返回含 `token`+`refreshToken`+`roles`。记 `TK`=token、`RT`=refreshToken。

- [ ] **Step 3: 无 token 401、有 token 200**

Run:
```bash
curl -s -o /dev/null -w "%{http_code}\n" http://localhost/api/system/user/page
curl -s -o /dev/null -w "%{http_code}\n" http://localhost/api/system/user/page -H "Authorization: Bearer $TK"
```
Expected: 401 / 200。（`/system/user/page` 若实际路径不同，换任一受保护 GET。）

- [ ] **Step 4: 刷新令牌换新 access**

Run:
```bash
curl -s -X POST http://localhost/api/auth/refresh -H 'Content-Type: application/json' -d "{\"refreshToken\":\"$RT\"}"
```
Expected: 返回新 `token`；用它访问受保护接口 200。

- [ ] **Step 5: 授权校验（角色级）**

用一个非 ADMIN 账号（种子有则用，无则临时造并绑非 ADMIN 角色）登录拿 token，访问加了 `@PreAuthorize("hasRole('ADMIN')")` 的端点（如合同 approve）：
Expected: 403（Result 形状）。admin token 访问同端点放行到业务逻辑。

- [ ] **Step 6: 审计真实操作人**

admin 登录后做一次写操作（改个字典）→ 查库 `SELECT create_by,update_by FROM sys_dict ORDER BY id DESC LIMIT 1`
Expected: 操作人为 `admin` 而非 `system`。

- [ ] **Step 7: 前端登录冒烟**

localhost 前端用 admin/zhyq@2026 登录 → 进首页 → 确认既有页面正常（token 存取、401 跳登录逻辑未破坏）。

- [ ] **Step 8: 无代码改动无需 commit（前端存 refreshToken 微调则单独提交）**

---

## Self-Review

- **Spec 覆盖**：依赖(Task1)；JwtService 双令牌(Task2)；角色查询+UserDetails(Task3)；过滤器+401/403(Task4)；SecurityConfig+删旧+AuthController 重写(Task5)；密码 V20+审计操作人+授权标注(Task6)；冒烟(Task7)。设计文档 §3.1~3.8、§4、§5、§6 均有对应任务。
- **占位符扫描**：V20 的 `<BCRYPT_HASH_OF_...>` 是**必须替换的占位**，Task6 Step1-2 给出生成方式与"提交前替换"要求——非空泛 TODO。TmpHash.java 明确标注用后即删。其余无占位。
- **类型一致**：`JwtService.generateAccess(Long,String,List<String>)`/`generateRefresh(Long,String)`/`parse`/`isRefresh` 跨 Task2→4→5 一致；`LoginUser(uid,username,password,roles)` 构造跨 Task3→4→5 一致；`SecurityUtils.current()/currentUsername()/currentUid()` Task5 定义、Task6 使用一致；`selectRoleCodesByUserId` Task3 定义与使用一致；`UserDetailsServiceImpl.loadUserByUsername` Task3 定义、Task5 refresh 使用一致。
- **已知需实测校准点（主动标注，非缺陷）**：① SecurityConfig permitAll 是否含 context-path `/api`（Task7 冒烟为准）；② 种子角色码实际值（Task1 Step1 确认）决定 hasRole 参数；③ sys_role 是否有 deleted/status 列（Task3 Step1 核实）；④ 受保护 GET 与写表的真实路径（Task7 就近替换）。均给了确认手段。
- **顺序依赖**：Task1 引入 security 后、Task5 SecurityConfig 落地前**不要起服务**（默认 basic auth 拦截），已在 Task1 Step4 标注。
- **与 #3 版本号协调**：#3 占 V19，本计划占 V20；若编码顺序调换需相应调整 Flyway 版本号。
