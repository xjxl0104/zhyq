# 统一文件上传服务 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 从零建一套统一的文件上传/下载/删除/列表能力(本地磁盘存储)+ 一个可复用 Vue 上传组件,供工单/合同/巡检/资料归档等模块复用。

**Architecture:** 新增后端模块 `com.zhyq.park.file`,含 `SysFile` 实体、`sys_file` 通用附件表(biz_type+biz_id 关联任意业务)、`FileStorageService`(磁盘 IO + 安全校验的纯逻辑抽出为可测静态方法)、`FileController`(REST)。文件落 `${zhyq.upload.path}`(默认 ./uploads),按 yyyy/MM 分目录,UUID 重命名。前端加 `api/file.js` 与 `components/FileUpload.vue`。

**Tech Stack:** Java 17 · Spring Boot 3.2.5 · MyBatis-Plus · MySQL 8 · Flyway · JUnit 5 · Vue 3.4 · Element Plus 2.7 · Axios。

## Global Constraints

- 后端命令一律在 `backend/` 目录跑;Maven wrapper 用 `./mvnw`(Windows: `mvnw.cmd`)。产物 finalName=`zhyq-park`。
- 新增 entity/controller/mapper/service 类名先查重(跨包重名 → `ConflictingBeanDefinition` 启动失败):`find backend -name '*.java' | sed 's|.*/||' | sort | uniq -d`。本计划已核实 SysFile/SysFileMapper/FileController/FileStorageService/FileUpload 均 0 冲突。
- Entity 继承 `com.zhyq.park.common.base.BaseEntity`,业务字段不写 id/tenantId/审计/version/deleted(MyMetaObjectHandler 自动填充)。
- 统一返回 `com.zhyq.park.common.result.Result<T>`;`Result.ok(data)` / `Result.fail(msg)`。列表分页用 `PageResult.of(total, records)`。
- 类型白名单(允许):jpg jpeg png gif webp pdf doc docx xls xlsx ppt pptx dwg dxf txt zip。拒绝可执行:exe jsp sh bat 等。
- 大小限制:max-file-size 20MB / max-request-size 50MB;后端二次校验。
- 文件名不信任原名,UUID 重命名;最终规范化路径必须落在上传根目录内(路径穿越防护)。
- Flyway 新迁移占 **V28**(已确认空闲),不改既有 V1–V27。
- 不改 `common/` 基座、`ZhyqParkApplication`;`application.yml` 只新增段落不动既有。
- 本次**不接入**具体业务页面(工单/合同等后续各自接入)。

---

## File Structure

**后端(新建)**
- `backend/src/main/resources/db/migration/V28__file.sql` — sys_file 建表
- `backend/src/main/java/com/zhyq/park/file/entity/SysFile.java` — 实体
- `backend/src/main/java/com/zhyq/park/file/mapper/SysFileMapper.java` — MyBatis-Plus mapper
- `backend/src/main/java/com/zhyq/park/file/service/FileStorageService.java` — 存储逻辑;安全校验纯函数抽为 static(可测)
- `backend/src/main/java/com/zhyq/park/file/controller/FileController.java` — REST 接口
- `backend/src/test/java/com/zhyq/park/file/FileStorageServiceTest.java` — 纯函数单测

**后端(修改)**
- `backend/src/main/resources/application.yml` — 新增 multipart + zhyq.upload 段
- `backend/src/main/java/com/zhyq/park/common/config/WebConfig.java` — 加 `/uploads/**` 静态资源映射(实现 `addResourceHandlers`)

**前端(新建)**
- `frontend/src/api/file.js` — 接口封装
- `frontend/src/components/FileUpload.vue` — 可复用上传组件

**部署(修改)**
- `docker-compose.full.yml` — 后端服务挂载 ./uploads 卷(持久化)
- `docs/DEPLOY.md` — 补一句上传目录挂载说明

任务顺序:V28 建表 → 实体/mapper → 存储服务(先纯函数 TDD)→ 配置 → controller → 静态映射 → 前端组件 → 部署收尾。

---

### Task 1: sys_file 建表迁移(V28)

**Files:**
- Create: `backend/src/main/resources/db/migration/V28__file.sql`

**Interfaces:**
- Produces: 表 `sys_file`,列 id/biz_type/biz_id/original_name/store_path/url/file_size/content_type/ext + 标准审计列;索引 `idx_biz(biz_type,biz_id)`。

- [ ] **Step 1: 写建表 SQL**

```sql
-- V28__file.sql 统一附件表
CREATE TABLE sys_file (
    id            BIGINT       NOT NULL AUTO_INCREMENT,
    biz_type      VARCHAR(32)  NULL COMMENT '业务类型 work_order/contract/patrol/building_doc',
    biz_id        BIGINT       NULL COMMENT '关联业务主键,可空(先传后关联)',
    original_name VARCHAR(255) NOT NULL COMMENT '原始文件名',
    store_path    VARCHAR(512) NOT NULL COMMENT '相对存储路径 yyyy/MM/uuid.ext',
    url           VARCHAR(512) NOT NULL COMMENT '访问URL',
    file_size     BIGINT       NOT NULL DEFAULT 0 COMMENT '字节数',
    content_type  VARCHAR(128) NULL COMMENT 'MIME',
    ext           VARCHAR(32)  NULL COMMENT '扩展名',
    tenant_id     BIGINT       NULL DEFAULT 1,
    create_by     VARCHAR(64)  NULL,
    create_time   DATETIME     NULL,
    update_by     VARCHAR(64)  NULL,
    update_time   DATETIME     NULL,
    version       INT          NOT NULL DEFAULT 1,
    deleted       TINYINT      NOT NULL DEFAULT 0,
    PRIMARY KEY (id),
    KEY idx_biz (biz_type, biz_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='统一附件表';
```

- [ ] **Step 2: 校验迁移文件命名与语法**

Run: `ls backend/src/main/resources/db/migration/V28__file.sql`
Expected: 文件存在。命名 `V28__file.sql` 双下划线,与既有 V1–V27 风格一致。

- [ ] **Step 3: 提交**

```bash
git add backend/src/main/resources/db/migration/V28__file.sql
git commit -m "feat(file): sys_file 通用附件表迁移 V28"
```

> 注:完整 Flyway 迁移验证放到 Task 5(controller 完成后随后端启动一起验证),此处不单独起库。

---

### Task 2: SysFile 实体 + Mapper

**Files:**
- Create: `backend/src/main/java/com/zhyq/park/file/entity/SysFile.java`
- Create: `backend/src/main/java/com/zhyq/park/file/mapper/SysFileMapper.java`

**Interfaces:**
- Consumes: `com.zhyq.park.common.base.BaseEntity`。
- Produces: `SysFile`(字段 bizType/bizId/originalName/storePath/url/fileSize/contentType/ext,均有 lombok getter/setter);`SysFileMapper extends BaseMapper<SysFile>`。

- [ ] **Step 1: 写实体**

```java
package com.zhyq.park.file.entity;

import com.baomidou.mybatisplus.annotation.TableName;
import com.zhyq.park.common.base.BaseEntity;
import lombok.Data;
import lombok.EqualsAndHashCode;

@Data
@EqualsAndHashCode(callSuper = true)
@TableName("sys_file")
public class SysFile extends BaseEntity {
    private String bizType;
    private Long bizId;
    private String originalName;
    private String storePath;
    private String url;
    private Long fileSize;
    private String contentType;
    private String ext;
}
```

- [ ] **Step 2: 写 mapper**

```java
package com.zhyq.park.file.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.zhyq.park.file.entity.SysFile;

public interface SysFileMapper extends BaseMapper<SysFile> {}
```

- [ ] **Step 3: 编译验证**

Run: `cd backend && ./mvnw -q compile`
Expected: BUILD SUCCESS,无编译错误。

- [ ] **Step 4: 提交**

```bash
git add backend/src/main/java/com/zhyq/park/file/entity/SysFile.java backend/src/main/java/com/zhyq/park/file/mapper/SysFileMapper.java
git commit -m "feat(file): SysFile 实体与 mapper"
```

---

### Task 3: FileStorageService — 安全校验纯函数(TDD)

先只做**可测的纯静态函数**:扩展名提取、白名单判断、UUID 文件名生成、相对路径构造、路径穿越防护。磁盘 IO 与 mapper 注入放 Task 4,保持本任务纯逻辑、无 Spring 上下文、快测。

**Files:**
- Create: `backend/src/main/java/com/zhyq/park/file/service/FileStorageService.java`
- Test: `backend/src/test/java/com/zhyq/park/file/FileStorageServiceTest.java`

**Interfaces:**
- Produces(本任务的 static 方法,供 Task 4 复用):
  - `static String extOf(String originalName)` — 返回小写扩展名(不含点),无扩展名返回 ""。
  - `static boolean isAllowed(String ext)` — ext 在白名单返回 true。
  - `static String newStoredName(String originalName, String uuid)` — 返回 `uuid.ext`(有扩展名时),无扩展名返回 `uuid`。
  - `static String buildRelativePath(java.time.LocalDate date, String storedName)` — 返回 `yyyy/MM/storedName`。
  - `static java.nio.file.Path resolveSafely(java.nio.file.Path root, String relative)` — 规范化后校验仍在 root 内,否则抛 `IllegalArgumentException`。
  - 常量 `static final java.util.Set<String> ALLOWED_EXT`。

- [ ] **Step 1: 写失败测试**

```java
package com.zhyq.park.file;

import com.zhyq.park.file.service.FileStorageService;
import org.junit.jupiter.api.Test;
import java.nio.file.Path;
import java.nio.file.Paths;
import java.time.LocalDate;
import static org.junit.jupiter.api.Assertions.*;

class FileStorageServiceTest {

    @Test
    void extOf_lowercasesAndStripsDot() {
        assertEquals("jpg", FileStorageService.extOf("Photo.JPG"));
        assertEquals("pdf", FileStorageService.extOf("a.b.pdf"));
        assertEquals("", FileStorageService.extOf("noext"));
    }

    @Test
    void isAllowed_acceptsWhitelistRejectsExecutable() {
        assertTrue(FileStorageService.isAllowed("png"));
        assertTrue(FileStorageService.isAllowed("dwg"));
        assertFalse(FileStorageService.isAllowed("exe"));
        assertFalse(FileStorageService.isAllowed("jsp"));
        assertFalse(FileStorageService.isAllowed(""));
    }

    @Test
    void newStoredName_usesUuidPlusExt() {
        assertEquals("abc123.jpg", FileStorageService.newStoredName("x.JPG", "abc123"));
        assertEquals("abc123", FileStorageService.newStoredName("noext", "abc123"));
    }

    @Test
    void buildRelativePath_yearMonthPrefixed() {
        assertEquals("2026/07/abc.png",
            FileStorageService.buildRelativePath(LocalDate.of(2026, 7, 24), "abc.png"));
    }

    @Test
    void resolveSafely_allowsInsideRoot() {
        Path root = Paths.get("/data/uploads");
        Path p = FileStorageService.resolveSafely(root, "2026/07/abc.png");
        assertTrue(p.normalize().startsWith(root.normalize()));
    }

    @Test
    void resolveSafely_rejectsTraversal() {
        Path root = Paths.get("/data/uploads");
        assertThrows(IllegalArgumentException.class,
            () -> FileStorageService.resolveSafely(root, "../../etc/passwd"));
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && ./mvnw -q -Dtest=FileStorageServiceTest test`
Expected: 编译失败 / FAIL —— `FileStorageService` 类或方法不存在。

- [ ] **Step 3: 写最小实现**

```java
package com.zhyq.park.file.service;

import java.nio.file.Path;
import java.time.LocalDate;
import java.time.format.DateTimeFormatter;
import java.util.Set;

/**
 * 文件存储:安全校验纯函数(static,可单测)。磁盘 IO 与持久化在 Task 4 补。
 */
public class FileStorageService {

    public static final Set<String> ALLOWED_EXT = Set.of(
        "jpg","jpeg","png","gif","webp",
        "pdf","doc","docx","xls","xlsx","ppt","pptx",
        "dwg","dxf","txt","zip");

    private static final DateTimeFormatter YM = DateTimeFormatter.ofPattern("yyyy/MM");

    public static String extOf(String originalName) {
        if (originalName == null) return "";
        int dot = originalName.lastIndexOf('.');
        if (dot < 0 || dot == originalName.length() - 1) return "";
        return originalName.substring(dot + 1).toLowerCase();
    }

    public static boolean isAllowed(String ext) {
        return ext != null && ALLOWED_EXT.contains(ext);
    }

    public static String newStoredName(String originalName, String uuid) {
        String ext = extOf(originalName);
        return ext.isEmpty() ? uuid : uuid + "." + ext;
    }

    public static String buildRelativePath(LocalDate date, String storedName) {
        return date.format(YM) + "/" + storedName;
    }

    public static Path resolveSafely(Path root, String relative) {
        Path normalizedRoot = root.normalize();
        Path resolved = normalizedRoot.resolve(relative).normalize();
        if (!resolved.startsWith(normalizedRoot)) {
            throw new IllegalArgumentException("非法路径: " + relative);
        }
        return resolved;
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd backend && ./mvnw -q -Dtest=FileStorageServiceTest test`
Expected: PASS,6 个测试全绿。

- [ ] **Step 5: 提交**

```bash
git add backend/src/main/java/com/zhyq/park/file/service/FileStorageService.java backend/src/test/java/com/zhyq/park/file/FileStorageServiceTest.java
git commit -m "feat(file): 存储安全校验纯函数 + 单测(白名单/UUID/路径穿越)"
```

---

### Task 4: 配置 + 存储 IO 实例方法

给 `FileStorageService` 补 Spring 组件能力:读配置根目录、落盘、删盘。实例方法调用 Task 3 的静态纯函数。

**Files:**
- Modify: `backend/src/main/resources/application.yml`(新增段)
- Modify: `backend/src/main/java/com/zhyq/park/file/service/FileStorageService.java`

**Interfaces:**
- Consumes: Task 3 的 static 方法。
- Produces(实例方法,供 Task 5 controller 用):
  - `StoredResult store(MultipartFile file)` — 校验类型/大小 → 落盘 → 返回 `StoredResult(storePath, url, ext, contentType, size, originalName)`。类型不合法抛 `com.zhyq.park.common.exception.BizException`。
  - `void deletePhysical(String storePath)` — 删磁盘文件,不存在则忽略。
  - `record StoredResult(String storePath, String url, String ext, String contentType, long size, String originalName)`。

- [ ] **Step 1: application.yml 新增段**

在文件末尾追加(缩进对齐现有 spring: 段;multipart 放 spring.servlet 下):

```yaml
spring:
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 50MB

zhyq:
  upload:
    path: ./uploads
    url-prefix: /uploads
```

> 若 `spring:` / `spring.servlet:` 已存在,合并到既有节点下,不要重复顶层键。

- [ ] **Step 2: 给 FileStorageService 加实例方法**

在 Task 3 的类里追加(保留已有 static 方法):

```java
    @org.springframework.beans.factory.annotation.Value("${zhyq.upload.path:./uploads}")
    private String uploadPath;

    @org.springframework.beans.factory.annotation.Value("${zhyq.upload.url-prefix:/uploads}")
    private String urlPrefix;

    // 20MB,与 application.yml 二次校验一致
    private static final long MAX_SIZE = 20L * 1024 * 1024;

    public record StoredResult(String storePath, String url, String ext,
                               String contentType, long size, String originalName) {}

    public StoredResult store(org.springframework.web.multipart.MultipartFile file) {
        if (file == null || file.isEmpty()) {
            throw new com.zhyq.park.common.exception.BizException("文件为空");
        }
        if (file.getSize() > MAX_SIZE) {
            throw new com.zhyq.park.common.exception.BizException("文件超过20MB上限");
        }
        String original = org.springframework.util.StringUtils.cleanPath(
            file.getOriginalFilename() == null ? "" : file.getOriginalFilename());
        String ext = extOf(original);
        if (!isAllowed(ext)) {
            throw new com.zhyq.park.common.exception.BizException("不允许的文件类型: " + ext);
        }
        String uuid = java.util.UUID.randomUUID().toString().replace("-", "");
        String stored = newStoredName(original, uuid);
        String relative = buildRelativePath(java.time.LocalDate.now(), stored);
        java.nio.file.Path root = java.nio.file.Paths.get(uploadPath).toAbsolutePath();
        java.nio.file.Path target = resolveSafely(root, relative);
        try {
            java.nio.file.Files.createDirectories(target.getParent());
            file.transferTo(target.toFile());
        } catch (java.io.IOException e) {
            throw new com.zhyq.park.common.exception.BizException("文件保存失败");
        }
        String url = urlPrefix + "/" + relative;
        return new StoredResult(relative, url, ext, file.getContentType(), file.getSize(), original);
    }

    public void deletePhysical(String storePath) {
        if (storePath == null || storePath.isBlank()) return;
        try {
            java.nio.file.Path root = java.nio.file.Paths.get(uploadPath).toAbsolutePath();
            java.nio.file.Path target = resolveSafely(root, storePath);
            java.nio.file.Files.deleteIfExists(target);
        } catch (Exception ignore) {
            // 物理文件删除失败不阻断逻辑删除
        }
    }
```

并给类加 `@org.springframework.stereotype.Service` 注解(类声明上方加一行 `@org.springframework.stereotype.Service`)。

- [ ] **Step 3: 确认 BizException 存在**

Run: `ls backend/src/main/java/com/zhyq/park/common/exception/BizException.java`
Expected: 文件存在。若不存在,改用项目实际业务异常类名(先 `find backend -name '*Exception.java'` 查,替换上面两处 `BizException`)。

- [ ] **Step 4: 编译 + 复跑纯函数测试**

Run: `cd backend && ./mvnw -q -Dtest=FileStorageServiceTest test`
Expected: BUILD SUCCESS,6 测试仍全绿(加实例方法不影响静态测试)。

- [ ] **Step 5: 提交**

```bash
git add backend/src/main/resources/application.yml backend/src/main/java/com/zhyq/park/file/service/FileStorageService.java
git commit -m "feat(file): 存储落盘/删除实例方法 + multipart 与 upload 配置"
```

---

### Task 5: FileController — 上传/批量/列表/删除

**Files:**
- Create: `backend/src/main/java/com/zhyq/park/file/controller/FileController.java`

**Interfaces:**
- Consumes: `FileStorageService.store/deletePhysical/StoredResult`(Task 4)、`SysFileMapper`(Task 2)、`Result`/`PageResult`。
- Produces: REST 端点 `POST /file/upload`、`POST /file/upload-batch`、`GET /file/list`、`DELETE /file/{id}`。

- [ ] **Step 1: 写 controller**

```java
package com.zhyq.park.file.controller;

import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import com.zhyq.park.common.result.Result;
import com.zhyq.park.file.entity.SysFile;
import com.zhyq.park.file.mapper.SysFileMapper;
import com.zhyq.park.file.service.FileStorageService;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.tags.Tag;
import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.multipart.MultipartFile;

import java.util.ArrayList;
import java.util.List;

@Tag(name = "文件上传")
@RestController
@RequestMapping("/file")
@RequiredArgsConstructor
public class FileController {

    private final FileStorageService storageService;
    private final SysFileMapper fileMapper;

    @Operation(summary = "上传单文件")
    @PostMapping("/upload")
    public Result<SysFile> upload(@RequestParam("file") MultipartFile file,
                                  @RequestParam(required = false) String bizType,
                                  @RequestParam(required = false) Long bizId) {
        return Result.ok(save(file, bizType, bizId));
    }

    @Operation(summary = "批量上传")
    @PostMapping("/upload-batch")
    public Result<List<SysFile>> uploadBatch(@RequestParam("files") MultipartFile[] files,
                                             @RequestParam(required = false) String bizType,
                                             @RequestParam(required = false) Long bizId) {
        List<SysFile> list = new ArrayList<>();
        for (MultipartFile f : files) {
            list.add(save(f, bizType, bizId));
        }
        return Result.ok(list);
    }

    @Operation(summary = "查业务对象附件列表")
    @GetMapping("/list")
    public Result<List<SysFile>> list(@RequestParam String bizType,
                                      @RequestParam Long bizId) {
        return Result.ok(fileMapper.selectList(new LambdaQueryWrapper<SysFile>()
            .eq(SysFile::getBizType, bizType)
            .eq(SysFile::getBizId, bizId)
            .orderByDesc(SysFile::getId)));
    }

    @Operation(summary = "删除附件")
    @DeleteMapping("/{id}")
    public Result<Void> remove(@PathVariable Long id) {
        SysFile f = fileMapper.selectById(id);
        if (f != null) {
            fileMapper.deleteById(id);          // 逻辑删除
            storageService.deletePhysical(f.getStorePath());
        }
        return Result.ok();
    }

    private SysFile save(MultipartFile file, String bizType, Long bizId) {
        FileStorageService.StoredResult r = storageService.store(file);
        SysFile sf = new SysFile();
        sf.setBizType(bizType);
        sf.setBizId(bizId);
        sf.setOriginalName(r.originalName());
        sf.setStorePath(r.storePath());
        sf.setUrl(r.url());
        sf.setFileSize(r.size());
        sf.setContentType(r.contentType());
        sf.setExt(r.ext());
        fileMapper.insert(sf);
        return sf;
    }
}
```

- [ ] **Step 2: 编译**

Run: `cd backend && ./mvnw -q compile`
Expected: BUILD SUCCESS。

- [ ] **Step 3: 启动后端验证 Flyway V28 迁移 + 接口存在**

Run: `cd backend && ./mvnw -q spring-boot:run`(需本地 MySQL 3316 起;等启动日志出现 Started;然后另开终端验证或看日志无 Flyway 报错后 Ctrl+C)
Expected: 日志含 `Migrating schema ... to version 28`,无迁移错误;`Started ZhyqParkApplication`。若无本地库,跳过运行、仅保证 compile 通过并在 review 时说明未做 live 验证。

- [ ] **Step 4: 提交**

```bash
git add backend/src/main/java/com/zhyq/park/file/controller/FileController.java
git commit -m "feat(file): 上传/批量/列表/删除 REST 接口"
```

---

### Task 6: 静态资源映射(/uploads/**)

让上传的文件可通过 URL 访问。注意后端 context-path 是 `/api`,故对外完整路径是 `/api/uploads/**`,前端 request baseURL 已是 `/api`,url 存相对 `/uploads/xxx` 即可正确拼接。

**Files:**
- Modify: `backend/src/main/java/com/zhyq/park/common/config/WebConfig.java`

**Interfaces:**
- Consumes: `zhyq.upload.path`、`zhyq.upload.url-prefix` 配置。
- Produces: 静态资源 handler,`/uploads/**` → 磁盘目录。

- [ ] **Step 1: 给 WebConfig 加 addResourceHandlers**

在 WebConfig 类中新增(与 addCorsMappings 并列;补 import `org.springframework.web.servlet.config.annotation.ResourceHandlerRegistry` 和 `org.springframework.beans.factory.annotation.Value`):

```java
    @org.springframework.beans.factory.annotation.Value("${zhyq.upload.path:./uploads}")
    private String uploadPath;

    @org.springframework.beans.factory.annotation.Value("${zhyq.upload.url-prefix:/uploads}")
    private String urlPrefix;

    @Override
    public void addResourceHandlers(
            org.springframework.web.servlet.config.annotation.ResourceHandlerRegistry registry) {
        String location = "file:" + java.nio.file.Paths.get(uploadPath).toAbsolutePath() + "/";
        registry.addResourceHandler(urlPrefix + "/**")
                .addResourceLocations(location);
    }
```

- [ ] **Step 2: 编译**

Run: `cd backend && ./mvnw -q compile`
Expected: BUILD SUCCESS。

- [ ] **Step 3: 提交**

```bash
git add backend/src/main/java/com/zhyq/park/common/config/WebConfig.java
git commit -m "feat(file): /uploads 静态资源映射"
```

---

### Task 7: 前端 api/file.js

**Files:**
- Create: `frontend/src/api/file.js`

**Interfaces:**
- Consumes: `@/utils/request`(baseURL `/api`,自动带 token)。
- Produces: `fileApi = { upload, uploadBatch, list, remove }`,`uploadUrl` 常量供 el-upload action 用。

- [ ] **Step 1: 写 api**

```js
import request from '@/utils/request'

// el-upload 的 action 直传地址(request baseURL 是 /api)
export const uploadUrl = '/api/file/upload'

export const fileApi = {
  upload(formData) {
    return request.post('/file/upload', formData, {
      headers: { 'Content-Type': 'multipart/form-data' }
    })
  },
  uploadBatch(formData) {
    return request.post('/file/upload-batch', formData, {
      headers: { 'Content-Type': 'multipart/form-data' }
    })
  },
  list(bizType, bizId) {
    return request.get('/file/list', { params: { bizType, bizId } })
  },
  remove(id) {
    return request.delete(`/file/${id}`)
  }
}
```

- [ ] **Step 2: 语法检查(可选)**

若前端有 lint:`cd frontend && npx eslint src/api/file.js`(无 lint 则跳过,肉眼确认 import 路径与既有 api 文件一致)。

- [ ] **Step 3: 提交**

```bash
git add frontend/src/api/file.js
git commit -m "feat(file): 前端文件接口封装"
```

---

### Task 8: 前端 FileUpload.vue 可复用组件

**Files:**
- Create: `frontend/src/components/FileUpload.vue`

**Interfaces:**
- Consumes: `fileApi`、`uploadUrl`(Task 7);el-upload(Element Plus 全局自动导入,无需手动 import)。
- Produces: 组件 `<FileUpload v-model="fileList" :biz-type="" :biz-id="" />`;`v-model` 为附件记录数组(SysFile 列表)。

- [ ] **Step 1: 写组件**

```vue
<script setup>
import { ref, watch } from 'vue'
import { ElMessage } from 'element-plus'
import { uploadUrl, fileApi } from '@/api/file'

const props = defineProps({
  modelValue: { type: Array, default: () => [] },
  bizType: { type: String, default: '' },
  bizId: { type: [Number, String], default: null },
  accept: { type: String, default: '' }
})
const emit = defineEmits(['update:modelValue'])

const fileList = ref([])

// 把外部传入的附件记录映射成 el-upload 需要的结构
watch(() => props.modelValue, (val) => {
  fileList.value = (val || []).map(f => ({ name: f.originalName, url: f.url, id: f.id, raw: f }))
}, { immediate: true })

const headers = { Authorization: `Bearer ${localStorage.getItem('zhyq_token') || ''}` }
const uploadData = { bizType: props.bizType, bizId: props.bizId }

function onSuccess(res) {
  // 后端 Result 结构 { code, message, data }
  if (res.code === 0 && res.data) {
    const next = [...props.modelValue, res.data]
    emit('update:modelValue', next)
  } else {
    ElMessage.error(res.message || '上传失败')
  }
}

function onError() {
  ElMessage.error('上传失败')
}

function beforeUpload(file) {
  const is20M = file.size / 1024 / 1024 <= 20
  if (!is20M) ElMessage.error('文件不能超过 20MB')
  return is20M
}

async function onRemove(uploadFile) {
  const id = uploadFile.id || (uploadFile.raw && uploadFile.raw.id)
  if (id) {
    try { await fileApi.remove(id) } catch (e) { /* 忽略,前端仍移除 */ }
    const next = props.modelValue.filter(f => f.id !== id)
    emit('update:modelValue', next)
  }
}
</script>

<template>
  <el-upload
    :action="uploadUrl"
    :headers="headers"
    :data="uploadData"
    :file-list="fileList"
    :accept="accept"
    :on-success="onSuccess"
    :on-error="onError"
    :before-upload="beforeUpload"
    :on-remove="onRemove"
    multiple
  >
    <el-button type="primary">选择文件</el-button>
    <template #tip>
      <div class="el-upload__tip">支持 jpg/png/pdf/doc/xls/dwg 等,单个不超过 20MB</div>
    </template>
  </el-upload>
</template>
```

- [ ] **Step 2: 肉眼确认**

确认 `<script setup>` 语法、`@/api/file` 路径、el-upload 属性名正确。组件不手动 import el-upload/el-button(项目 unplugin 全局自动导入)。

- [ ] **Step 3: 提交**

```bash
git add frontend/src/components/FileUpload.vue
git commit -m "feat(file): 可复用 FileUpload 组件"
```

---

### Task 9: 部署持久化 + 文档

**Files:**
- Modify: `docker-compose.full.yml`
- Modify: `docs/DEPLOY.md`

**Interfaces:**
- Produces: 后端容器挂载 `./uploads` 卷,重建不丢文件。

- [ ] **Step 1: docker-compose.full.yml 后端服务加卷**

在 backend 服务下增加(与既有 backend 服务的 environment 同级):

```yaml
    volumes:
      - ./uploads:/app/uploads
```

并确认后端容器工作目录下 `./uploads` 与 `zhyq.upload.path` 默认值一致(默认 `./uploads`,容器内工作目录 /app,故卷挂到 /app/uploads)。若容器工作目录不是 /app,改成对应路径,或用环境变量 `ZHYQ_UPLOAD_PATH` 指定绝对路径 `/app/uploads`。

- [ ] **Step 2: DEPLOY.md 补说明**

在 DEPLOY.md 合适位置追加一段:

```markdown
### 文件上传持久化
上传文件存于后端容器 /app/uploads,已通过 docker-compose.full.yml 挂载宿主 ./uploads 持久化。
换机/重建容器前备份宿主 ./uploads 目录即可保留附件。
```

- [ ] **Step 3: 提交**

```bash
git add docker-compose.full.yml docs/DEPLOY.md
git commit -m "chore(file): uploads 目录容器卷持久化 + 部署文档"
```

---

## 验收清单(全部任务完成后)

- [ ] `cd backend && ./mvnw -q test` 全绿(含 FileStorageServiceTest 6 项)。
- [ ] `cd backend && ./mvnw -q compile` BUILD SUCCESS。
- [ ] 后端启动日志含 Flyway migrate to version 28,无错误(有本地库时)。
- [ ] 手测:上传合法文件返回 url,磁盘 ./uploads/yyyy/MM/ 下生成文件;上传 .exe 被拒;超 20MB 被拒;list/delete 正常。
- [ ] 前端 build 通过;FileUpload 组件可选文件、显进度、成功回填、删除。
- [ ] 未接入具体业务页面(符合本次范围);接入是后续工单/合同等各自的工作。
