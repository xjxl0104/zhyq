# ver4.2 RBAC Management Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让超级管理员在页面内创建多角色用户、创建新的超级管理员，并为普通角色配置菜单与按钮权限。

**Architecture:** 沿用 `sys_user -> sys_user_role -> sys_role -> sys_role_menu -> sys_menu.perm`，新增两个事务服务分别管理用户角色和角色权限；Controller 只负责鉴权与 REST 适配。`admin` 角色由后端强制保护，关联变更失败时整体回滚。

**Tech Stack:** Java 17 · Spring Boot 3.2.5 · Spring Security · MyBatis-Plus · MySQL 8 · Flyway · JUnit 5 · Mockito · Vue 3 · Element Plus · pnpm · Vite。

## Global Constraints

- 基线固定为 `origin/ver4.1` 的 `9f0fd64`，目标分支固定为 `ver4.2`。
- 一个用户可选择多个启用角色；多角色权限取并集。
- `admin` 是受保护角色编码；只有 `ROLE_admin` 可授予或撤销，且系统始终至少保留一个启用超级管理员。
- 不新增用户级权限表；不重做全站静态菜单体系。
- 关系表写操作必须事务化，失败整体回滚。
- 新增生产方法前先写失败测试并观察预期失败。
- Flyway 使用 `V31__rbac_management.sql`，不得修改 V1–V30。
- 后端命令在 `backend/` 执行；前端命令在 `frontend/` 执行。
- 新增 Java 类先执行重复类名检查。

---

## File Structure

**Backend create**

- `backend/src/main/resources/db/migration/V31__rbac_management.sql` — 关系唯一索引与新增管理权限点。
- `backend/src/main/java/com/zhyq/park/system/dto/SysUserSaveRequest.java` — 用户写请求。
- `backend/src/main/java/com/zhyq/park/system/dto/SysUserDetailResponse.java` — 用户详情与角色 ID。
- `backend/src/main/java/com/zhyq/park/system/dto/RolePermissionRequest.java` — 角色权限写请求。
- `backend/src/main/java/com/zhyq/park/system/dto/UserRoleLabel.java` — 当前页用户角色聚合结果。
- `backend/src/main/java/com/zhyq/park/system/entity/SysUserRole.java` — 用户角色关系实体。
- `backend/src/main/java/com/zhyq/park/system/entity/SysRoleMenu.java` — 角色权限关系实体。
- `backend/src/main/java/com/zhyq/park/system/mapper/SysUserRoleMapper.java` — 用户角色查询与写入。
- `backend/src/main/java/com/zhyq/park/system/mapper/SysRoleMenuMapper.java` — 角色权限查询与写入。
- `backend/src/main/java/com/zhyq/park/system/service/CurrentUserContext.java` — 当前 JWT 是否包含 `ROLE_admin`。
- `backend/src/main/java/com/zhyq/park/system/service/UserManagementService.java` — 用户与角色事务。
- `backend/src/main/java/com/zhyq/park/system/service/RoleManagementService.java` — 角色与权限事务。
- `backend/src/test/java/com/zhyq/park/system/service/UserManagementServiceTest.java`
- `backend/src/test/java/com/zhyq/park/system/service/RoleManagementServiceTest.java`
- `backend/src/test/java/com/zhyq/park/system/service/CurrentUserContextTest.java`

**Backend modify**

- `backend/src/main/java/com/zhyq/park/system/entity/SysUser.java` — 非持久化 `roleNames`。
- `backend/src/main/java/com/zhyq/park/system/controller/SysUserController.java` — DTO、事务服务与角色权限标注。
- `backend/src/main/java/com/zhyq/park/system/controller/SysRoleController.java` — 事务服务与权限配置接口。
- `backend/src/test/java/com/zhyq/park/auth/SysUserControllerSecurityTest.java` — 新接口方法级鉴权。

**Frontend modify**

- `frontend/src/api/system.js` — 用户详情、角色权限查询和保存。
- `frontend/src/views/system/User.vue` — 多角色选择、详情回填、角色展示。
- `frontend/src/views/system/Role.vue` — 权限树与 `admin` 保护交互。

**Docs modify**

- `docs/DEPLOY.md` — ver4.2 数据迁移和重新登录说明。

---

### Task 1: 数据模型、迁移与当前用户上下文

**Files:**

- Create: `backend/src/main/resources/db/migration/V31__rbac_management.sql`
- Create: `backend/src/main/java/com/zhyq/park/system/entity/SysUserRole.java`
- Create: `backend/src/main/java/com/zhyq/park/system/entity/SysRoleMenu.java`
- Create: `backend/src/main/java/com/zhyq/park/system/mapper/SysUserRoleMapper.java`
- Create: `backend/src/main/java/com/zhyq/park/system/mapper/SysRoleMenuMapper.java`
- Create: `backend/src/main/java/com/zhyq/park/system/dto/UserRoleLabel.java`
- Create: `backend/src/main/java/com/zhyq/park/system/service/CurrentUserContext.java`
- Test: `backend/src/test/java/com/zhyq/park/system/service/CurrentUserContextTest.java`

**Interfaces:**

- Produces: `CurrentUserContext.isAdmin(): boolean`。
- Produces: `SysUserRoleMapper.selectRoleIdsByUserId(Long)`、`hasRoleCode(Long,String)`、`countActiveAdminUsers()`、`selectRoleLabelsByUserIds(Collection<Long>)`。
- Produces: `SysRoleMenuMapper.selectMenuIdsByRoleId(Long)`。

- [x] **Step 1: 写当前用户上下文失败测试**

```java
@Test
void ROLE_admin被识别为超级管理员() {
    SecurityContextHolder.getContext().setAuthentication(
        new UsernamePasswordAuthenticationToken("root", null,
            List.of(new SimpleGrantedAuthority("ROLE_admin"))));
    assertTrue(new CurrentUserContext().isAdmin());
}

@Test
void 普通权限不被识别为超级管理员() {
    SecurityContextHolder.getContext().setAuthentication(
        new UsernamePasswordAuthenticationToken("operator", null,
            List.of(new SimpleGrantedAuthority("system:user:add"))));
    assertFalse(new CurrentUserContext().isAdmin());
}
```

- [x] **Step 2: 验证 RED**

Run: `mvn -q -Dtest=CurrentUserContextTest test`
Expected: FAIL，`CurrentUserContext` 尚不存在。

- [x] **Step 3: 实现上下文、实体与 Mapper**

```java
@Component
public class CurrentUserContext {
    public boolean isAdmin() {
        Authentication auth = SecurityContextHolder.getContext().getAuthentication();
        return auth != null && auth.getAuthorities().stream()
                .anyMatch(a -> "ROLE_admin".equals(a.getAuthority()));
    }
}
```

关系实体使用 `@TableName`、`@TableId(type = IdType.AUTO)`；Mapper 使用注解 SQL，`countActiveAdminUsers()` 必须只统计 `u.status=1`、用户/角色未删除且角色启用的不同用户。

- [x] **Step 4: 写 V31**

```sql
DELETE a FROM sys_user_role a
JOIN sys_user_role b ON a.user_id=b.user_id AND a.role_id=b.role_id AND a.id>b.id;
DELETE a FROM sys_role_menu a
JOIN sys_role_menu b ON a.role_id=b.role_id AND a.menu_id=b.menu_id AND a.id>b.id;
ALTER TABLE sys_user_role ADD UNIQUE KEY uk_user_role (user_id, role_id);
ALTER TABLE sys_role_menu ADD UNIQUE KEY uk_role_menu (role_id, menu_id);

INSERT INTO sys_menu (parent_id,name,type,perm,sort,visible,status,tenant_id,create_by,create_time,version,deleted)
SELECT 0,'用户-分配角色',3,'system:user:role',0,0,1,1,'system',NOW(),1,0
WHERE NOT EXISTS (SELECT 1 FROM sys_menu WHERE perm='system:user:role' AND deleted=0);
INSERT INTO sys_menu (parent_id,name,type,perm,sort,visible,status,tenant_id,create_by,create_time,version,deleted)
SELECT 0,'角色-配置权限',3,'system:role:permission',0,0,1,1,'system',NOW(),1,0
WHERE NOT EXISTS (SELECT 1 FROM sys_menu WHERE perm='system:role:permission' AND deleted=0);

INSERT INTO sys_role_menu (role_id, menu_id)
SELECT r.id,m.id FROM sys_role r JOIN sys_menu m
  ON m.perm IN ('system:user:role','system:role:permission') AND m.deleted=0
WHERE r.code='admin' AND r.deleted=0
  AND NOT EXISTS (SELECT 1 FROM sys_role_menu rm WHERE rm.role_id=r.id AND rm.menu_id=m.id);
```

- [x] **Step 5: 验证 GREEN 和编译**

Run: `mvn -q -Dtest=CurrentUserContextTest test && mvn -q -DskipTests compile`
Expected: PASS，退出码 0。

- [x] **Step 6: 提交**

```bash
git add backend/src/main/resources/db/migration/V31__rbac_management.sql \
  backend/src/main/java/com/zhyq/park/system/entity \
  backend/src/main/java/com/zhyq/park/system/mapper \
  backend/src/main/java/com/zhyq/park/system/dto/UserRoleLabel.java \
  backend/src/main/java/com/zhyq/park/system/service/CurrentUserContext.java \
  backend/src/test/java/com/zhyq/park/system/service/CurrentUserContextTest.java
git commit -m "feat(rbac): add role relation model and migration"
```

### Task 2: 用户与多角色事务服务

**Files:**

- Create: `backend/src/main/java/com/zhyq/park/system/dto/SysUserSaveRequest.java`
- Create: `backend/src/main/java/com/zhyq/park/system/dto/SysUserDetailResponse.java`
- Create: `backend/src/main/java/com/zhyq/park/system/service/UserManagementService.java`
- Modify: `backend/src/main/java/com/zhyq/park/system/entity/SysUser.java`
- Test: `backend/src/test/java/com/zhyq/park/system/service/UserManagementServiceTest.java`

**Interfaces:**

- Consumes: Task 1 Mappers 与 `CurrentUserContext`。
- Produces: `create(SysUserSaveRequest): Long`、`update(SysUserSaveRequest): void`、`delete(Long): void`、`get(Long): SysUserDetailResponse`、`fillRoleNames(List<SysUser>): void`。

- [x] **Step 1: 写用户服务失败测试**

测试必须覆盖以下断言：

```java
assertTrue(passwordEncoder.matches("secret88", insertedUser.getPassword()));
verify(userRoleMapper, times(2)).insert(any(SysUserRole.class));
assertThrows(BizException.class, () -> service.create(requestWithAdminRole(false)));
assertDoesNotThrow(() -> service.create(requestWithAdminRole(true)));
assertThrows(BizException.class, () -> service.update(removesLastAdminRequest()));
assertThrows(BizException.class, () -> service.delete(LAST_ADMIN_USER_ID));
```

另写测试验证：密码留空更新不覆盖、角色整体替换、非法/停用角色拒绝、用户名重复拒绝、当前页角色名称批量填充。

- [x] **Step 2: 验证 RED**

Run: `mvn -q -Dtest=UserManagementServiceTest test`
Expected: FAIL，DTO 与服务尚不存在。

- [x] **Step 3: 实现 DTO**

```java
@Data
public class SysUserSaveRequest {
    private Long id;
    private String username;
    private String password;
    private String nickname;
    private Long deptId;
    private Long postId;
    private String phone;
    private String email;
    private Integer gender;
    private String avatar;
    private Integer userType;
    private Integer status;
    private String remark;
    private List<Long> roleIds;
}

@Data
@AllArgsConstructor
public class SysUserDetailResponse {
    private SysUser user;
    private List<Long> roleIds;
}
```

- [x] **Step 4: 实现事务服务**

```java
@Service
@RequiredArgsConstructor
public class UserManagementService {
    private static final String ADMIN_CODE = "admin";
    private final SysUserMapper userMapper;
    private final SysRoleMapper roleMapper;
    private final SysUserRoleMapper userRoleMapper;
    private final PasswordEncoder passwordEncoder;
    private final CurrentUserContext currentUserContext;

    @Transactional(rollbackFor = Exception.class)
    public Long create(SysUserSaveRequest request) {
        requireNewUsername(request.getUsername());
        if (!StringUtils.hasText(request.getPassword())) throw new BizException("请设置初始密码");
        List<SysRole> roles = requireEnabledRoles(request.getRoleIds());
        guardAdminRoleChange(false, containsAdmin(roles));
        SysUser user = toNewUser(request);
        user.setPassword(passwordEncoder.encode(request.getPassword()));
        userMapper.insert(user);
        replaceRoles(user.getId(), roles);
        return user.getId();
    }

    @Transactional(rollbackFor = Exception.class)
    public void update(SysUserSaveRequest request) {
        SysUser existing = requireUser(request.getId());
        List<SysRole> roles = requireEnabledRoles(request.getRoleIds());
        boolean oldAdmin = userRoleMapper.hasRoleCode(existing.getId(), ADMIN_CODE) > 0;
        boolean newAdmin = containsAdmin(roles);
        guardAdminRoleChange(oldAdmin, newAdmin);
        if (oldAdmin && Integer.valueOf(1).equals(existing.getStatus())
                && (!newAdmin || !Integer.valueOf(1).equals(request.getStatus()))) guardLastAdmin();
        applyEditableFields(existing, request);
        existing.setPassword(StringUtils.hasText(request.getPassword())
                ? passwordEncoder.encode(request.getPassword()) : null);
        userMapper.updateById(existing);
        replaceRoles(existing.getId(), roles);
    }

    @Transactional(rollbackFor = Exception.class)
    public void delete(Long id) {
        SysUser existing = requireUser(id);
        if (userRoleMapper.hasRoleCode(id, ADMIN_CODE) > 0) {
            if (!currentUserContext.isAdmin()) throw new BizException("只有超级管理员可以删除超级管理员");
            if (Integer.valueOf(1).equals(existing.getStatus())) guardLastAdmin();
        }
        userRoleMapper.delete(new LambdaQueryWrapper<SysUserRole>().eq(SysUserRole::getUserId, id));
        userMapper.deleteById(id);
    }

    public SysUserDetailResponse get(Long id) {
        return new SysUserDetailResponse(requireUser(id), userRoleMapper.selectRoleIdsByUserId(id));
    }

    public void fillRoleNames(List<SysUser> users) {
        if (users.isEmpty()) return;
        Map<Long, List<String>> labels = userRoleMapper
                .selectRoleLabelsByUserIds(users.stream().map(SysUser::getId).toList()).stream()
                .collect(Collectors.toMap(UserRoleLabel::getUserId,
                    row -> Arrays.asList(row.getRoleNames().split(","))));
        users.forEach(user -> user.setRoleNames(labels.getOrDefault(user.getId(), List.of())));
    }

    private void guardAdminRoleChange(boolean oldAdmin, boolean newAdmin) {
        if (oldAdmin != newAdmin && !currentUserContext.isAdmin())
            throw new BizException("只有超级管理员可以授予或撤销超级管理员角色");
    }

    private void guardLastAdmin() {
        if (userRoleMapper.countActiveAdminUsers() <= 1)
            throw new BizException("系统必须至少保留一个启用的超级管理员");
    }

    private void requireNewUsername(String username) {
        if (!StringUtils.hasText(username)) throw new BizException("请输入账号");
        Long count = userMapper.selectCount(new LambdaQueryWrapper<SysUser>()
                .eq(SysUser::getUsername, username));
        if (count > 0) throw new BizException("账号已存在");
    }

    private SysUser requireUser(Long id) {
        SysUser user = id == null ? null : userMapper.selectById(id);
        if (user == null) throw new BizException("用户不存在");
        return user;
    }

    private List<SysRole> requireEnabledRoles(List<Long> roleIds) {
        List<Long> ids = roleIds == null ? List.of() : roleIds.stream().filter(Objects::nonNull).distinct().toList();
        if (ids.isEmpty()) throw new BizException("请至少选择一个角色");
        List<SysRole> roles = roleMapper.selectBatchIds(ids);
        if (roles.size() != ids.size() || roles.stream().anyMatch(r -> !Integer.valueOf(1).equals(r.getStatus())))
            throw new BizException("角色不存在或已停用");
        return roles;
    }

    private boolean containsAdmin(List<SysRole> roles) {
        return roles.stream().anyMatch(r -> ADMIN_CODE.equals(r.getCode()));
    }

    private SysUser toNewUser(SysUserSaveRequest request) {
        SysUser user = new SysUser();
        applyEditableFields(user, request);
        user.setUsername(request.getUsername().trim());
        if (user.getStatus() == null) user.setStatus(1);
        if (user.getUserType() == null) user.setUserType(1);
        return user;
    }

    private void applyEditableFields(SysUser user, SysUserSaveRequest request) {
        user.setNickname(request.getNickname());
        user.setDeptId(request.getDeptId());
        user.setPostId(request.getPostId());
        user.setPhone(request.getPhone());
        user.setEmail(request.getEmail());
        user.setGender(request.getGender());
        user.setAvatar(request.getAvatar());
        user.setUserType(request.getUserType());
        user.setStatus(request.getStatus());
        user.setRemark(request.getRemark());
    }

    private void replaceRoles(Long userId, List<SysRole> roles) {
        userRoleMapper.delete(new LambdaQueryWrapper<SysUserRole>().eq(SysUserRole::getUserId, userId));
        roles.forEach(role -> userRoleMapper.insert(new SysUserRole(null, userId, role.getId())));
    }
}
```

`replaceRoles` 先规范化角色 ID，再验证角色数量与启用状态；涉及 `admin` 归属变化时调用 `CurrentUserContext.isAdmin()`；撤销有效管理员前调用 `countActiveAdminUsers()`。

- [x] **Step 5: 验证 GREEN**

Run: `mvn -q -Dtest=UserManagementServiceTest test`
Expected: PASS。

- [x] **Step 6: 提交**

```bash
git add backend/src/main/java/com/zhyq/park/system/dto \
  backend/src/main/java/com/zhyq/park/system/entity/SysUser.java \
  backend/src/main/java/com/zhyq/park/system/service/UserManagementService.java \
  backend/src/test/java/com/zhyq/park/system/service/UserManagementServiceTest.java
git commit -m "feat(rbac): manage users with multiple roles"
```

### Task 3: 用户 REST 接口与方法级鉴权

**Files:**

- Modify: `backend/src/main/java/com/zhyq/park/system/controller/SysUserController.java`
- Modify: `backend/src/test/java/com/zhyq/park/auth/SysUserControllerSecurityTest.java`

**Interfaces:**

- Consumes: `UserManagementService`。
- Produces: 设计文档中 `/system/user` 的详情、新增、修改、删除行为。

- [x] **Step 1: 先改安全测试为期望接口并观察失败**

```java
@Test
@WithMockUser(authorities = {"system:user:add", "system:user:role"})
void 新增用户_同时具备新增与分配角色权限_放行() {
    when(service.create(any())).thenReturn(9L);
    assertDoesNotThrow(() -> controller.add(new SysUserSaveRequest()));
}

@Test
@WithMockUser(authorities = "system:user:add")
void 新增用户_缺少分配角色权限_拒绝() {
    assertThrows(AccessDeniedException.class,
        () -> controller.add(new SysUserSaveRequest()));
}
```

修改和删除分别验证权限组合及服务委托。

- [x] **Step 2: 验证 RED**

Run: `mvn -q -Dtest=SysUserControllerSecurityTest test`
Expected: FAIL，Controller 仍接收 `SysUser`。

- [x] **Step 3: 实现 Controller**

```java
@PreAuthorize("hasAuthority('system:user:add') and hasAuthority('system:user:role')")
@PostMapping
public Result<Long> add(@RequestBody SysUserSaveRequest request) {
    return Result.ok(userManagementService.create(request));
}

@PreAuthorize("hasAuthority('system:user:edit') and hasAuthority('system:user:role')")
@PutMapping
public Result<Void> update(@RequestBody SysUserSaveRequest request) {
    userManagementService.update(request);
    return Result.ok();
}
```

分页查询保留现有过滤，并在返回前调用 `fillRoleNames`；详情和删除委托事务服务。

- [x] **Step 4: 验证 GREEN**

Run: `mvn -q -Dtest=SysUserControllerSecurityTest,UserManagementServiceTest test`
Expected: PASS。

- [x] **Step 5: 提交**

```bash
git add backend/src/main/java/com/zhyq/park/system/controller/SysUserController.java \
  backend/src/test/java/com/zhyq/park/auth/SysUserControllerSecurityTest.java
git commit -m "feat(rbac): expose transactional user role APIs"
```

### Task 4: 角色权限事务服务与接口

**Files:**

- Create: `backend/src/main/java/com/zhyq/park/system/dto/RolePermissionRequest.java`
- Create: `backend/src/main/java/com/zhyq/park/system/service/RoleManagementService.java`
- Modify: `backend/src/main/java/com/zhyq/park/system/controller/SysRoleController.java`
- Test: `backend/src/test/java/com/zhyq/park/system/service/RoleManagementServiceTest.java`
- Create: `backend/src/test/java/com/zhyq/park/auth/SysRoleControllerSecurityTest.java`

**Interfaces:**

- Produces: `add(SysRole): Long`、`update(SysRole)`、`delete(Long)`、`getMenuIds(Long)`、`saveMenuIds(Long,List<Long>)`。

- [x] **Step 1: 写角色服务与 Controller 失败测试**

```java
assertEquals(List.of(10L, 11L), service.getMenuIds(ROLE_ID));
assertDoesNotThrow(() -> service.saveMenuIds(NORMAL_ROLE_ID, List.of(10L, 11L)));
assertThrows(BizException.class, () -> service.saveMenuIds(ADMIN_ROLE_ID, List.of()));
assertThrows(BizException.class, () -> service.delete(ADMIN_ROLE_ID));
assertThrows(BizException.class, () -> service.update(changedAdminRole()));
```

Controller 测试验证 `PUT /{id}/menu-ids` 需要 `system:role:permission`。

- [x] **Step 2: 验证 RED**

Run: `mvn -q -Dtest=RoleManagementServiceTest,SysRoleControllerSecurityTest test`
Expected: FAIL，服务、DTO 和接口尚不存在。

- [x] **Step 3: 实现角色事务服务**

```java
@Data
public class RolePermissionRequest {
    private List<Long> menuIds;
}

@Service
@RequiredArgsConstructor
public class RoleManagementService {
    private static final String ADMIN_CODE = "admin";
    private final SysRoleMapper roleMapper;
    private final SysMenuMapper menuMapper;
    private final SysUserRoleMapper userRoleMapper;
    private final SysRoleMenuMapper roleMenuMapper;

    @Transactional(rollbackFor = Exception.class)
    public Long add(SysRole role) {
        requireUniqueCode(role.getCode(), null);
        roleMapper.insert(role);
        return role.getId();
    }

    @Transactional(rollbackFor = Exception.class)
    public void update(SysRole role) {
        SysRole existing = requireRole(role.getId());
        rejectProtectedAdmin(existing);
        requireUniqueCode(role.getCode(), role.getId());
        roleMapper.updateById(role);
    }

    @Transactional(rollbackFor = Exception.class)
    public void delete(Long roleId) {
        SysRole role = requireRole(roleId);
        rejectProtectedAdmin(role);
        userRoleMapper.delete(new LambdaQueryWrapper<SysUserRole>().eq(SysUserRole::getRoleId, roleId));
        roleMenuMapper.delete(new LambdaQueryWrapper<SysRoleMenu>().eq(SysRoleMenu::getRoleId, roleId));
        roleMapper.deleteById(roleId);
    }

    public List<Long> getMenuIds(Long roleId) {
        requireRole(roleId);
        return roleMenuMapper.selectMenuIdsByRoleId(roleId);
    }

    @Transactional(rollbackFor = Exception.class)
    public void saveMenuIds(Long roleId, List<Long> menuIds) {
        SysRole role = requireRole(roleId);
        rejectProtectedAdmin(role);
        List<Long> normalized = normalizeAndValidateMenus(menuIds);
        roleMenuMapper.delete(new LambdaQueryWrapper<SysRoleMenu>()
            .eq(SysRoleMenu::getRoleId, roleId));
        normalized.forEach(menuId -> roleMenuMapper.insert(new SysRoleMenu(null, roleId, menuId)));
    }

    private SysRole requireRole(Long id) {
        SysRole role = id == null ? null : roleMapper.selectById(id);
        if (role == null) throw new BizException("角色不存在");
        return role;
    }

    private void rejectProtectedAdmin(SysRole role) {
        if (ADMIN_CODE.equals(role.getCode())) throw new BizException("超级管理员角色受系统保护");
    }

    private void requireUniqueCode(String code, Long excludeId) {
        if (!StringUtils.hasText(code)) throw new BizException("请输入角色编码");
        LambdaQueryWrapper<SysRole> query = new LambdaQueryWrapper<SysRole>()
                .eq(SysRole::getCode, code.trim()).ne(excludeId != null, SysRole::getId, excludeId);
        if (roleMapper.selectCount(query) > 0) throw new BizException("角色编码已存在");
    }

    private List<Long> normalizeAndValidateMenus(List<Long> menuIds) {
        List<Long> ids = menuIds == null ? List.of() : menuIds.stream().filter(Objects::nonNull).distinct().toList();
        if (ids.isEmpty()) return ids;
        List<SysMenu> menus = menuMapper.selectBatchIds(ids);
        if (menus.size() != ids.size() || menus.stream().anyMatch(m -> !Integer.valueOf(1).equals(m.getStatus())))
            throw new BizException("菜单权限不存在或已停用");
        return ids;
    }
}
```

`update` 对 `admin` 完全拒绝；普通角色校验编码唯一。`delete` 清理用户角色和角色权限关系后逻辑删除角色。

- [x] **Step 4: 实现 Controller 权限接口**

```java
@PreAuthorize("hasAuthority('system:role:query')")
@GetMapping("/{id}/menu-ids")
public Result<List<Long>> menuIds(@PathVariable Long id) {
    return Result.ok(roleManagementService.getMenuIds(id));
}

@PreAuthorize("hasAuthority('system:role:permission')")
@PutMapping("/{id}/menu-ids")
public Result<Void> saveMenuIds(@PathVariable Long id,
                                @RequestBody RolePermissionRequest request) {
    roleManagementService.saveMenuIds(id, request.getMenuIds());
    return Result.ok();
}
```

- [x] **Step 5: 验证 GREEN**

Run: `mvn -q -Dtest=RoleManagementServiceTest,SysRoleControllerSecurityTest test`
Expected: PASS。

- [x] **Step 6: 提交**

```bash
git add backend/src/main/java/com/zhyq/park/system/dto/RolePermissionRequest.java \
  backend/src/main/java/com/zhyq/park/system/service/RoleManagementService.java \
  backend/src/main/java/com/zhyq/park/system/controller/SysRoleController.java \
  backend/src/test/java/com/zhyq/park/system/service/RoleManagementServiceTest.java \
  backend/src/test/java/com/zhyq/park/auth/SysRoleControllerSecurityTest.java
git commit -m "feat(rbac): configure role permissions safely"
```

### Task 5: 用户管理前端多角色交互

**Files:**

- Modify: `frontend/src/api/system.js`
- Modify: `frontend/src/views/system/User.vue`

**Interfaces:**

- Consumes: `GET /system/user/{id}` 与 `GET /system/role/list`。
- Produces: `form.roleIds: number[]`，提交时与用户字段一起发送。

- [x] **Step 1: 增加 API**

```javascript
export const userApi = {
  page: (params) => request.get('/system/user/page', { params }),
  get: (id) => request.get(`/system/user/${id}`),
  add: (data) => request.post('/system/user', data),
  update: (data) => request.put('/system/user', data),
  remove: (id) => request.delete(`/system/user/${id}`)
}
```

- [x] **Step 2: 增加角色展示与多选**

```vue
<el-table-column label="角色" min-width="180">
  <template #default="{ row }">
    <el-tag v-for="name in row.roleNames || []" :key="name" class="role-tag">{{ name }}</el-tag>
  </template>
</el-table-column>

<el-form-item label="角色" prop="roleIds">
  <el-select v-model="form.roleIds" multiple collapse-tags style="width:100%">
    <el-option v-for="role in roles" :key="role.id" :value="role.id"
      :label="role.code === 'admin' ? `${role.name}（超级管理员）` : role.name" />
  </el-select>
</el-form-item>
```

规则：`roleIds` 至少一项；`onMounted` 同时加载用户和角色；编辑时 `await userApi.get(row.id)` 后回填 `detail.user` 与 `detail.roleIds`。

- [x] **Step 3: 构建验证**

Run: `pnpm build`
Expected: Vite build 成功，退出码 0。

- [x] **Step 4: 提交**

```bash
git add frontend/src/api/system.js frontend/src/views/system/User.vue
git commit -m "feat(rbac): select multiple roles for users"
```

### Task 6: 角色权限树前端

**Files:**

- Modify: `frontend/src/api/system.js`
- Modify: `frontend/src/views/system/Role.vue`

**Interfaces:**

- Consumes: `menuApi.list()`、`roleApi.menuIds(id)`、`roleApi.saveMenuIds(id, menuIds)`。

- [x] **Step 1: 增加角色权限 API**

```javascript
menuIds: (id) => request.get(`/system/role/${id}/menu-ids`),
saveMenuIds: (id, menuIds) => request.put(`/system/role/${id}/menu-ids`, { menuIds })
```

- [x] **Step 2: 增加权限入口与受保护角色状态**

```vue
<el-tag v-if="row.code === 'admin'" type="danger">系统保护</el-tag>
<el-button link type="primary" :disabled="row.code === 'admin'"
  @click="openPermission(row)">配置权限</el-button>
```

`admin` 行的编辑和删除也必须禁用。

- [x] **Step 3: 增加权限树弹窗**

```vue
<el-dialog v-model="permission.visible" title="配置角色权限" width="680px">
  <el-tree ref="permissionTreeRef" :data="menuTree" node-key="id"
    show-checkbox default-expand-all :props="{ label: 'name', children: 'children' }">
    <template #default="{ data }">
      <span>{{ data.name }}<small v-if="data.perm">（{{ data.perm }}）</small></span>
    </template>
  </el-tree>
  <template #footer>
    <el-button @click="permission.visible=false">取消</el-button>
    <el-button type="primary" @click="savePermission">保存</el-button>
  </template>
</el-dialog>
```

`openPermission` 并行加载菜单和已选 ID；`savePermission` 合并 `getCheckedKeys()` 与 `getHalfCheckedKeys()` 后去重提交。

- [x] **Step 4: 构建验证**

Run: `pnpm build`
Expected: Vite build 成功，退出码 0。

- [x] **Step 5: 提交**

```bash
git add frontend/src/api/system.js frontend/src/views/system/Role.vue
git commit -m "feat(rbac): add role permission tree"
```

### Task 7: 文档、全量验证与发布准备

**Files:**

- Modify: `docs/DEPLOY.md`
- Modify: `docs/superpowers/plans/2026-08-02-ver4.2-rbac-management.md` — 勾选已执行步骤。

- [x] **Step 1: 更新部署说明**

写明：切换 `ver4.2` 后 Flyway 自动执行 V31；已有管理员关系保留；角色变更后用户重新登录；回滚代码不回滚已执行迁移。

- [x] **Step 2: 全量后端验证**

Run: `mvn -q test && mvn -q package`
Expected: 全部测试通过，jar 构建成功。

- [x] **Step 3: 全量前端验证**

Run: `pnpm build`
Expected: Vite build 成功。

- [x] **Step 4: 静态与图谱复核**

```bash
find backend -name '*.java' | sed 's|.*/||' | sort | uniq -d
git diff --check origin/ver4.1...HEAD
rg -n '(password\s*=|secret\s*=|token\s*=)' backend frontend --glob '!**/target/**' --glob '!**/dist/**'
codegraph sync backend
codegraph sync frontend
```

Expected: 无新增重复类名、无 diff 格式错误、无新增明文凭证，CodeGraph 状态最新。

- [x] **Step 5: Docker 可用时执行全栈冒烟**

Run: `docker version`；成功时运行 `docker compose -f docker-compose.full.yml up -d --build`，登录 admin，创建普通多角色用户和新的 admin 用户，分别重新登录验证权限；完成后仅停止本次 compose 服务，不删除卷。

执行记录：仓库未提供生产 `.env`，为避免复用固定容器名影响其他本地实例，改用明确命名的临时 MySQL 8 容器 + 已打包后端完成同等 API 冒烟，并分别完成前后端 Docker 镜像构建；临时容器和检查镜像均已清理。

- [x] **Step 6: 提交收尾文档**

```bash
git add docs/DEPLOY.md docs/superpowers/plans/2026-08-02-ver4.2-rbac-management.md
git commit -m "docs: document ver4.2 RBAC deployment"
```

- [ ] **Step 7: 发布**

重新检查 `git status -sb` 和 `git diff origin/ver4.1...HEAD`，然后推送：

```bash
git push -u origin ver4.2
```

Expected: GitHub 新增或更新 `xjxl0104/zhyq` 的 `ver4.2` 分支。
