# 园区全民营销 · 三个会话的工作汇总（接力用）

> 生成：2026-09-21，作者：sell1（Claude 主脑会话）
> 用途：把 **sell1（功能实现）+ sell2（盲审与修复）+ Codex（全链路补齐）** 三方的成果合成一份接力文档，交给新会话继续。
> 仓库 `github.com/xjxl0104/zhyq`，工作树 `/Users/aole/Documents/cc/zhyq`，分支 `feat/crm-marketing`。
> **现状：545 后端测试全绿（本文件写作时实跑核实）；37 改 + 61 新增全部未提交、未推送。**

---

## 一、一句话现状

三条线的产出已**全部汇入同一个工作树**：全民营销（伙伴小程序 + 后台 + 云仓 ERP 开放接口）**三阶段功能完成**，盲审发现的 13 个阻塞**已修 12 个**，Codex 又推进了**云仓端小程序 + ERP 可靠性 + 月账单 + 云仓结算**（V60–V63）。
**只剩 C2/C3/C7/C8 一组（实体透传/校验/PII）+ Codex 列的 5 项外部接入；提交前必须走项目纪律（不推 main）。**

---

## 二、三方各自做了什么

### 2.1 sell1（Claude 主脑，09-20 15:41 – 09-21 03:56，后又同步）

**阶段 A · 后台模块**（已 commit `d136812`）
- 迁移 `V57`（19 表 + `sys_audit` + 老表加列 + 岗位/评级/23 条规则参数种子）、`V58`（31 权限点 + 3 角色）
- 后端 `com.zhyq.park.marketing`：`engine`（级差引擎/池计算/阶梯解析）、`service`（计佣/合同/加盟/锁客/伙伴/提现/导入/晋升/审计）、`listener`（租赁一次性佣金 + 到账解冻）、`job`（解冻/锁定到期/晋升复核/合同提醒）、14 个 Controller
- 前端 `views/crm/marketing/` 14 页 + `api/marketing.js` + 路由菜单
- **修了存量 bug**：`PaymentService.收款` 从未发布 `PaymentReceived` 事件

**阶段 B · 伙伴小程序**（未提交）
- `marketing/mp/`：`MpAuthService`（微信登录 + mock 开关）、`MpAuthController`、`MpMeController`、`MpBizController`
- `JwtAuthFilter` 双向隔离（`mp:` token ↔ 后台 token 互斥）
- 全新 uni-app 工程 `miniapp/`，伙伴端 11 页，H5 + mp-weixin 均可构建

**阶段 C · 云仓 ERP 开放接口**（未提交）
- `V59`：凭证 / 货主编码映射 / 同步日志 / nonce 表
- `open/`：HMAC 验签（±300s + nonce 防重放 + 常量时间比较）、订单入账（复用计佣）、`order-event` / `batch` / `ping` / `contract`
- 后台 `Erp.vue` + `docs/marketing/ERP接入契约.md`
- 扮演云仓推 13 种场景全部符合契约

**修复阶段**（借 sell2 的盲审，未提交）
- B1 引擎超池（`allocated` 累计 + 尾差封顶）、B2 提现整额锁行、B3 并发提现拒绝、B4 `settle`/`void` 加 `sign=1`、B5 `voidUnsettledBySource`、B6 三处 `REQUIRES_NEW`、B7 按 `bill.source` 分流、B8 `MAX_CHAIN_DEPTH=64`、M2 → `V60`

### 2.2 sell2（Claude 执行手，09-21 01:05 – 03:50）

> 前 7 小时空转（反复调用不存在的工具名），此后 3 小时交付四块，**已全部并入 sell1 工作区**（`收敛核对指南.md` 第二版核实：11 个前端页面逐字节相同）。

| 块 | 文件 | 价值 |
|---|---|---|
| T1 引擎第二组测试 | `CommissionEngineTest`（26 用例） | **抓到 B1 超池**（池 1.25 拆出 1.26）——出库单佣金正是这个量级 |
| T14 盲审 | `~/Documents/cc/T14-盲审报告.md`（13🔴 / 34🟡 / ~40🟢）+ `t14-raw.md` | 5 路并行只读审查，两路独立命中标 ★ |
| 状态机守卫 | `support/WrapperAssert` + `MktTransitionGuardTest`（29 断言） | **能查 Wrapper 内容**，抓到 B4/B5/S11；sell1 原有测试只 mock 返回值抓不到 |
| 前端修复 | 11 页 + `api/marketing.js`（+101/−51）+ `api/__tests__/marketing.spec.js` | 关掉 B11/B12/B13 与 F1–F7 |

**sell2 独有的洞察**（新会话值得继承）：状态机测试不查 Wrapper 就抓不到"实现比规范宽"这类 bug；`AFTER_COMMIT` 里调 `REQUIRED` 是假事务。

### 2.3 Codex（全链路补齐，09-21 之后）

**伙伴小程序真实登录**
- `MpAuthService` 可注入 `code2session`；真实模式强制 `encryptedData + iv`，解密失败不落库，session_key 5 分钟一次性
- `MpAuthController.bindPhone` 按 `isMockLogin()` 分流明文/加密两条路
- 登录页已接 `uni.login` + `getPhoneNumber`

**云仓身份与商家小程序**（全新）
- 新增 `wh:{warehouseId}` 身份 + `WhAuthContext` + `/wh/v1/**` 隔离
- `marketing/wh/`：7 个类（登录/加盟/看板/ERP 自助/财务/进度）
- 小程序新增 **9 个云仓页面**（`warehouse-*`），独立 `wh_token` 不覆盖 `mp_token`
- 查询从 token 推 `warehouse_id`，过滤伙伴手机号/佣金/他仓数据

**ERP 可靠性**（`V61`）
- 新增事件 Inbox、死信、未映射队列、对账快照四张表
- `ErpEventContract` 强校验；沙箱接口改 dry-run 不产生正式佣金
- 重试 / 死信 / 6 小时断连恢复 / 对账 2% 阈值 的服务与定时任务

**财务与结算**（`V62`/`V63`）
- `V62` 服务费月账单（`billing_key=contract:{id}:service:{date}` 幂等）+ `marketing/finance/`
- `V63` 云仓结算快照 + 明细 + 直签平台费到账表 + `marketing/settlement/`
- 结算读对账快照，差异 ≥2% 冻结；直签按 `payment_no` 幂等，只对 `SOURCE_PLATFORM_FEE` 解冻佣金

---

## 三、当前真实状态（已核实）

| 项 | 状态 |
|---|---|
| 后端测试 | **545 绿 / 0 失败 / 1 跳过**（修完 B9/B10/C5 后实跑；Codex 交接时是 541） |
| 迁移 | V57–V63（V57/58 阶段 A、V59 ERP、V60 锁、V61 可靠性、V62 账单、V63 结算） |
| 后端包 | `engine` `service` `listener` `job` `controller` `mp` `open` `wh` `finance` `settlement` |
| 前端 | `views/crm/marketing/` 15 页（含 `Erp.vue`） |
| 小程序 | 伙伴端 11 页 + 云仓端 9 页 |
| 工作树 | **37 改 + 61 新增，全部未提交、未推送** |
| 本次收尾 | B9 加固 + C5 导入上限 + 4 个新测试（545 绿） |
| 13 个阻塞 | **已修 12**：B1–B8 + M2（sell1）、**B9/B10/C5（本次收尾）**；**剩 C2/C3/C7/C8 一组** |

---

## 四、还剩什么

### 4.1 已知缺陷（优先，`收敛核对指南.md` 第五节 + `T14-盲审报告.md`）

| 编号 | 问题 | 状态 |
|---|---|---|
| **B9** | 起草合同可被客户端指定 `partnerId`/`grade`（越权改佣金归属与比例） | ✅ **已修**：Codex 加了 `CreateReq` 白名单 DTO；sell1 又在 `MktServiceContractService.createDraft` 里改成**一律以客户档案为准**（即使传了也覆盖），并加测试 `createDraftIgnoresClientSuppliedPartnerAndGrade` |
| **B10** | 新增云仓写 JSON 空串必失败（MySQL 3140） | ✅ **已修**：`MktWarehouseOnboardingService.apply` 空串归一 null（Codex 做） |
| **C5** | 导入无大小/扩展名/行数上限，可 OOM 共用服务器 | ✅ **已修**：`MktReferralOrderImportService` 加 10MB / 5000 行 / `.xlsx|.xls` 白名单（无扩展名不拦，交 POI 判断），3 个测试 |
| **C2/C3/C7/C8** | 三处实体透传（模板/云仓/评级）、份额子集校验、推荐人不校验状态、Promoter 返回 PII（openid/deviceId） | ❌ **未修**，见盲审报告，估 $80–150 |

### 4.2 Codex 列的 5 项外部接入（`补齐开发交接` §后续接入事项）
1. ERP retry 的 `RECEIVED` 状态接正式重放 worker（现有人工重放入口）
2. 直签平台费到账金额与 `platformFeeModel` 应付金额做**足额校验**（现已做幂等 + 正数 + 来源隔离）
3. 通知接口接正式通知表（现返回安全空分页）
4. **真实微信 AppID/AppSecret + 真实 ERP 沙箱联调**
5. 多联系人场景需另建联系人表（现 `crm_warehouse.contact_openid` 单联系人）

### 4.3 其他
- **多项目数据范围**（盲审 C1）：全后端无租户/project 数据范围，营销所有 `/{id}` 可跨项目。项目基线如此，建议在 `docs/memory` 明确"阶段 A 单园区"。
- 25 个页面**没有人用眼睛看过**（本机无 Chrome，只做编译 + 接口联调）。
- `Astra Ultra` 外部审查因容量不足未启动（Codex 文档提及）。

---

## 五、提交纪律（重要）

工作树现在混了三件事，**建议分 3 次提交**（`收敛核对指南.md` §四）：

1. `feat(marketing): 阶段 B 伙伴小程序 + 阶段 C 云仓/ERP 骨架`（`mp/` `wh/` `open/` `miniapp/` V59 + 前端 13 页）
2. `fix(marketing): 修复佣金引擎尾差、提现锁定、结算 sign 过滤、事件分流等 9 项`（B1–B8 + V60）
3. `feat(marketing): ERP 可靠性 + 月账单 + 云仓结算 + 测试`（V61–V63 + `finance/` `settlement/` + 5 个测试文件）

**提交前**（CLAUDE.md）：后端 `mvn -B test-compile`（**不是** `compile`，服务器构建会连测试一起编）、前端 `pnpm build`。
**不要推 main** —— 这批含财务金额与状态流转，按项目纪律要停下让用户确认。
**合并前**：查生产库 `flyway_schema_history` 最大版本（应 V56）、确认 `crm_customer` 仍为空表、按 CLAUDE.md 附备份命令。

---

## 六、起点：三条线各自的原始文档

| 文档 | 位置 | 是什么 |
|---|---|---|
| 功能接手报告 | `~/Documents/cc/全民营销-接手报告-2026-09-21.md`（仓库同名 `docs/marketing/`） | sell1 写的，阶段 A/B/C 逐文件说明 + 拍板 8 条 + 环境手册 |
| 补齐开发交接 | `~/Documents/cc/全民营销-补齐开发交接-2026-09-21.md` | Codex 写的，本轮的完成度与后续接入 |
| 收敛核对指南 | `~/Documents/cc/收敛核对指南.md` | sell2 写的第二版，B1–B13 修复核实清单 |
| 盲审报告 | `~/Documents/cc/T14-盲审报告.md` + `t14-raw.md` | 13🔴 / 34🟡 逐条带文件行号与修法 |
| 统筹方案 | `~/Documents/cc/统筹方案-全链路补齐.md` | sell2 对补齐计划的依赖图与 Wave 0 分析 |
| 业务规范 | 仓库 `docs/marketing/PARK-MKT-001-开发方案-V1.2.md` | **真相源** |
| 设计与计划 | 仓库 `docs/superpowers/{specs,plans}/2026-09-21-crm-marketing-completion*` | Codex 的补齐设计与 8 任务计划 |
| ERP 契约 | 仓库 `docs/marketing/ERP接入契约.md` | 给云仓的对外接口契约 |
| 协作看板 | `~/Documents/cc/协作看板.md` | sell1↔sell2 通信日志 |

---

## 七、环境速查

```sh
# 编译 / 测试
export JAVA_HOME=/opt/homebrew/opt/openjdk@17; export PATH="$JAVA_HOME/bin:$PATH"
MVN=/Users/aole/tools/maven-current/bin/mvn        # 仓库 docs/memory 里的旧路径已失效
cd backend && "$MVN" -o -q compile && "$MVN" -o test          # 全量 545 绿
cd frontend && pnpm build && pnpm test
cd miniapp && pnpm install && pnpm build:h5 && pnpm build:mp-weixin

# 本地服务(docker start zhyq-mysql 先)
/tmp/zhyq-dev.sh     # 后端 8090 + 后台 5273 + 小程序 H5 5274/mp/
/tmp/zhyq-stop.sh
# 后台 admin / zhyq@2026;小程序登录页任意字符当微信身份(mock 模式)
```
**注意**：`zhyq-sell2` 工作区已转只读，不要再往它写东西（其内容已是本工作树的真子集）。
