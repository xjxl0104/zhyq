# ver6.8 发布说明

## 版本关系

- `ver6.8` 直接继承已验收的 `ver6.7`。
- `ver6.7` 可追溯到远端 `ver6.6` 精确提交 `a5b419827605efcc76fcc46fc4d87f3444aeb5b0`。

## 毛玻璃表面

- 新增 `GlassSurface`，提供 `upload` 与 `card` 两个视觉变体。
- 装饰光团和遮罩不接收指针事件、不进入可访问性树，真实业务控件保持在内容层。
- 上传变体使用 18 秒、仅改变 transform 的环境光旋转；卡片变体保持静态渐变，避免同时运行大量循环动画。
- `prefers-reduced-motion: reduce` 下停止旋转；不支持 `backdrop-filter` 时使用不透明浅色回退。
- 视觉方向参考用户提供、标注为 “From Uiverse.io by monkey_8812” 的示例，并按本项目亮色企业后台重新设计。

## 上传入口覆盖

以下入口保留原 `el-upload`、鉴权、校验、文件列表、解析、错误处理和提交逻辑，仅增加视觉容器：

1. 通用附件上传。
2. 自动售货机 Excel 导入。
3. 应收明细 Excel 导入。
4. 责任单位 Excel 导入。
5. 全局建议反馈截图。
6. 我的建议截图与普通附件。

## 展示卡片覆盖

- 工作台：顶部待办指标与经营概览卡。
- 应用中心：“我的常用”与“全部应用”两个展示分区。

表格、查询栏、编辑表单、预警/待办/快捷入口和其他高密度统计卡不应用毛玻璃。

## 兼容性

- 无后端、数据库、权限、路由或业务数据流变更。
- 文件状态、错误文案和成功反馈仍由原业务组件提供，毛玻璃不是状态的唯一提示。

## 导航与品牌动效增强

- 按用户提供的完整 React Bits StrokeText / GooeyNav 源码重新移植到 Vue，替换此前的简化动画。
- 标题保留“澳乐智慧云仓系统”，不显示建筑 Logo；引入 GSAP，使用实际 SVG 字形边界测量、1.6 秒逐字描边、0.05 秒错开、0.2 秒填色间隔及 0.8 秒从左向右填充。首次出现及悬停时播放，重播不重建 SVG。
- 点击保留原版 15 个 20px 粒子、90→10px 聚合轨迹、旋转/缩放双层关键帧及 600ms / 300ms 时间参数，不再缩小为右侧散点。气泡和脉冲高亮共用滤镜，产生黏连效果。
- 为适配浅色侧栏，原版黑底亮色混合改为 7px 模糊与透明度阈值合成，保留彩色粒子；高亮为实色靛蓝、白字，无原先的玻璃渐变/边框/阴影。文字位于特效之上。
- 保留悬停、键盘焦点和当前菜单反馈；粒子始终锚定本次点击的菜单，不随之后的悬停漂移。支持菜单展开、滚动、窗口尺寸变化和侧栏折叠；隐藏叶子回退到可见父级。
- 减少动态效果偏好下保持静态；组件卸载时清理 GSAP 时间线、监听器、观察器和定时器。

参考：[React Bits 导航](https://reactbits.dev/)、[Stroke Text](https://reactbits.dev/text-animations/stroke-text)。

## 验证命令

本次源码移植：11 个测试文件、49 项测试全部通过，生产构建通过。浏览器已检查标题描边与完成状态、点击气泡的中间帧以及选中文字对比度。构建仍有既有的 Sass 旧 API、第三方纯函数注释和大体积 chunk 警告。本地业务接口返回既有 500 错误，本次验证不代表后端数据加载正常。

```bash
cd frontend
pnpm test -- --run
pnpm build
```
