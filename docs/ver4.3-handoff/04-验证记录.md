# 验证记录

## 已完成验证

| 检查 | 结果 |
|---|---|
| 后端全量测试 | 175 项，0 失败、0 错误、1 项按设计跳过（本地工作簿专项测试） |
| 用户真实物业工作簿 | 专项测试 1 项通过：9 条业务行，27 列、总计及账户格式校验通过；工作簿未加入 Git |
| 前端全量测试 | 3 个测试文件、9 项测试全部通过 |
| 前端生产构建 | 2,550 个模块完成构建；仅第三方注释、Sass 旧 API 和 chunk 大小提示，无构建错误 |
| Docker Compose 配置 | `docker-compose.yml` 与 `docker-compose.full.yml` 均通过 `config --quiet` |
| Flyway 干净库 | MySQL 独立临时库从空库迁移到 v33；9 张应收/售货机业务表、2 张共享导入审计表、13 项新增权限存在 |
| Flyway 升级库 | `origin/ver4.2` 先迁移到 v31，再由 ver4.3 应用 V32/V33；用户与角色关联数量保持不变 |
| CodeGraph | 已同步至当前源码：435 个文件、10,311 个节点、18,411 条边，状态为最新 |
| Git 敏感文件检查 | 用户工作簿、微信文件路径和 `.env` 均未被跟踪 |

## 后端重点覆盖

- 27 列精确表头、公式缓存读取、总计核对和真实工作簿解析。
- AES-GCM 加解密、账号掩码、账号公式审计清理和原始工作簿不落盘。
- 主数据绑定存在性、园区归属、合同租户、合同房间及空间树关系。
- 免租、递增、折扣、租金/物业 OFFSET 抵扣、物业免缴、周期免租和分账金额。
- 导入修订稳定键、来源版本、快照恢复、乐观锁、账单历史检查、生成/撤销行锁及旧批次撤销拒绝。
- 手工新增/编辑字段白名单和删除关联清理。
- 售货机五类模板、解析、业务键更新、旧值恢复、软删唯一键和旧批次撤销拒绝。
- 应收和售货机的拆分 RBAC 边界。

## 发布前最后检查

```bash
cd backend && mvn test
cd ../frontend && pnpm test && pnpm build
cd .. && git diff --check
cd backend && npx -y @colbymchenry/codegraph sync && npx -y @colbymchenry/codegraph status
git status --short
```
