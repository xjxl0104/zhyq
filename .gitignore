# ---- Java / Maven ----
target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar
*.log

# ---- IDE ----
.idea/
*.iml
.vscode/
.DS_Store

# ---- Node / Frontend ----
node_modules/
dist/
dist-ssr/
*.local
.pnpm-store/

# ---- Env ----
.env
.env.local
.env.*
!.env.example
backend/.env

# ---- Secrets / Certs ----
*.pem
*.key
*.p12
*.jks
*.keystore
*-key.json
credentials.json

# ---- 运行时上传文件 ----
backend/uploads/
uploads/

# ---- 服务器本地运维脚本 ----
# 只存在于服务器 /opt/zhyq,内含部署路径与 webhook 密钥,仓库公开故不入库。
# 正规带 HMAC 校验的 webhook 实现见 backend common/event/HmacWebhookDispatcher.java
deploy.sh
webhook-listener.sh

# ---- 服务器本地数据目录(只存在于 /opt/zhyq) ----
# backups/ 是改库前的 mysqldump(含账号哈希),import/ 是待导入数据包与脚本(含银行账号等业务数据)。
# 仓库公开,一律不入库;流程见 docs/memory/zhyq-data-import-runbook.md
backups/
import/

# Claude Code 本机产物(勿提交)
.claude/worktrees/
.claude/launch.json
.claude/settings.local.json
